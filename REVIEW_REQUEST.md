# 검토 요청문

다른 세션(페이블 등)에 검토를 시킬 때 아래를 그대로 붙여 넣는다.
검토가 끝나면 나온 것을 이 저장소에 반영하고, **이 문서와 `ARCHITECTURE.md` 도 같이 고친다.**

---

## 붙여 넣을 것

```
수시 상담 보드를 검토해 줘. 전남 나주고 3학년실에서 9월 원서철에 실제로 쓸 도구다.

■ 먼저 읽을 것 (이 차례로)

  ARCHITECTURE.md        저장소 전체 구조·자료 흐름·설계 원칙. 여기부터.
  board/CONTRACT.md      상담 보드 파트별 계약과 지켜야 할 것
  board/SETUP.md         설치 절차 (교사가 실제로 밟는 길)

설명을 따로 물어보지 말고 이 셋을 읽어. 코드보다 이 문서가 먼저다.

■ 돌려 볼 것

  https://maechorom31-pixel.github.io/univ/board.html?demo=1     교사
  https://maechorom31-pixel.github.io/univ/student.html?demo=1   학생

`?demo=1` 은 지어낸 이름으로 모든 화면이 도는 보기용 자료다. 저장까지 눌러 볼 수 있다.
실제 학생 자료는 교사 구글시트에만 있고 저장소에는 없다.

  node board/match.test.mjs       node board/stats.test.mjs
  node board/confidence.test.mjs  node board/parse.test.mjs

■ 세 관점으로 봐 줘

1. 학생 — 폰으로, 옆에 선생님 없이, 아무 설명 없이 링크만 받은 사람
   · 처음 열었을 때 무엇을 해야 하는지 알겠는가
   · 「70%컷 2.96 / 내 환산 3.17」을 보고 혼자 잘못된 결론을 내리지 않는가
   · 12월에 합격 발표를 보고 결과를 적는 흐름이 자연스러운가
   · 겁을 주거나, 반대로 근거 없이 안심시키는 곳은 없는가

2. 교사 — 9월 원서철과 12월 발표철, 학생 121명 × 여섯 칸
   · 열자마자 「오늘 뭘 해야 하는지」가 보이는가
   · 상담 한 판(학생 고르기 → 카드 열기 → 순위 바꾸기 → 메모)이 몇 번의 조작인가
   · 121명 규모에서 무너지는 곳은 없는가 (데모는 4명이라 안 드러난다)
   · 진학 대장·지원 결과 보고서가 실제로 결재에 올릴 만한가

3. 개발자
   · 조용히 틀리는 곳 — 값을 버리거나, 잘못 세거나, 안 보여 주고 넘어가는 곳
   · 오류·빈 자료·중복·이상한 입력에서 무너지는 곳
   · 개인정보 경계를 넘는 코드 (ARCHITECTURE.md §1)
   · 문서와 코드가 어긋난 곳

■ 이 설계는 일부러 이렇게 한 것이니 「고쳐라」 하지 말 것

  · 학생 이름·성적은 저장소에 절대 안 들어온다. 결합은 브라우저 안에서만.
  · 잠금은 배포 주소다. 구글 계정으로 안 잠근다 (선생님이 기기를 옮겨 다닌다).
  · p값·신뢰구간을 내지 않는다. 입결은 요약값이라 개별 점수가 없다.
  · 애매하면 잇지 않는다. 오연결이 미연결보다 나쁘다.
  · 차이값(내 등급 − 컷)을 계산하지 않는다. 잣대가 다른 값이다.
  · CI 없이 사람이 스크립트를 돌린다. 원본이 해마다 바뀌는 엑셀·PDF라서다.

이 원칙 자체가 틀렸다고 보면 그건 말해 줘. 다만 「관례와 다르다」는 이유만으로는 말고,
이 학교 이 상황에서 왜 틀렸는지로 말해 줘.

■ 내가 이미 아는 약점 — 여기 시간 쓰지 말고, 더 나은 답이 있을 때만 말해 줘

  · 카드 상세가 2,985px 다. 1440×900 에서 30% 만 보인다. 구역 차례를 다시 볼 여지가 있다
  · 실제 즐겨찾기 엑셀로는 아직 못 돌려 봤다 (개인정보라 저장소에 없다).
    지어낸 121명 × 6칸 픽스처로는 돌려 봤다 — 파싱 726건·연결 100%
  · Apps Script 를 실제 배포해 본 적이 없다.
    특히 `설정` 탭 계정 잠금은 「액세스: 모든 사용자」 배포에서 접속자 계정을 알 수 없어
    작동하지 않을 것으로 본다. 코드가 그 경우를 가려내 사유를 적어 주도록 해 두었으나,
    실제로 확인한 것은 아니다
  · 데모 자료가 4명뿐이라 규모 문제가 안 드러난다.
    121명 첫 토큰 발급의 시트 왕복은 한 번 쓰기로 줄여 두었으나 실측하지 않았다

■ 내놓을 것

관점마다 나눠서, 심각한 것부터.

  무엇이   한 줄
  어디서   파일:줄 또는 화면 이름
  왜 문제  실제로 무슨 일이 벌어지는지. 「모범 사례와 다름」 말고
  어떻게   고칠 방향. 코드까지는 아니어도 된다

고칠 것이 없으면 없다고 해 줘. 억지로 채우지 마.
문서(ARCHITECTURE.md · CONTRACT.md)가 코드와 어긋난 곳도 같은 무게로 봐 줘.
```

---

## 왜 이렇게 쓰나

- **설명을 프롬프트에 넣지 않는다.** `ARCHITECTURE.md` 가 그 일을 하라고 있는 문서다.
  프롬프트에 다시 쓰면 두 벌이 되고, 반드시 갈라진다.
- **일부러 그렇게 한 것을 미리 못 박는다.** 안 그러면 검토의 절반이
  「구글 계정으로 잠그세요」·「p값을 넣으세요」·「CI 를 붙이세요」로 채워진다.
- **아는 약점을 먼저 밝힌다.** 이미 아는 것을 다시 찾아오는 데 예산을 쓰지 않게.
- **「모범 사례와 다름」을 미리 막는다.** 이 도구는 교무실 한 곳에서 쓰는 것이고,
  일반적인 웹 서비스의 관례가 여기서는 손해인 경우가 많다.
- **없으면 없다고 하라고 적는다.** 검토는 채우려 들면 소음이 된다.
