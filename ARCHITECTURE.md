# 저장소 구조와 설계

> 이 문서는 **저장소 전체를 한눈에 보는 지도**다. 개별 도구의 세부는 각자 문서에 있다.
>
> | 여기서 다루는 것 | 따로 있는 문서 |
> |---|---|
> | 무엇이 왜 이렇게 놓여 있나 · 자료가 어디서 와서 어디로 가나 · 지켜야 할 것 | 상담 보드 파트 계약 [`board/CONTRACT.md`](board/CONTRACT.md) |
> | | 상담 보드 설치 [`board/SETUP.md`](board/SETUP.md) |
> | | 모집요강 파싱 계획 [`PLAN_2027.md`](PLAN_2027.md) |
> | | 또래비교 설계 [`tools/DESIGN.md`](tools/DESIGN.md) |
> | | 쓰는 사람용 안내 [`README.md`](README.md) |
> | | 다른 세션에 검토 시킬 때 [`REVIEW_REQUEST.md`](REVIEW_REQUEST.md) |
>
> **고칠 때는 이 문서와 실제 파일을 같이 고친다.** 구조가 바뀌었는데 여기가 옛말이면,
> 다음 사람이 이 문서를 믿고 잘못된 곳을 뒤진다.

---

## 0. 무엇을 만드는 곳인가

전남 나주고등학교 3학년실에서 쓰는 대입 도구 모음이다. 만드는 사람도, 쓰는 사람도
같은 교무실 안에 있다. 그래서 두 가지가 다른 무엇보다 앞선다.

1. **틀린 값을 보여 주느니 안 보여 준다.** 상담에서 나온 숫자는 학생의 원서로 이어진다.
   못 붙인 것은 「연결 안 됨」이라 적고, 못 읽은 칸은 원문 그대로 남기고,
   추측한 값에는 추측이라고 쓴다.
2. **학생 이름과 성적은 저장소에 들어오지 않는다.** public 저장소이고 GitHub Pages 로
   공개 중이다.

---

## 1. 두 층 — 공개와 개인

이 경계가 이 저장소 설계의 뼈대다. 넘는 코드를 쓰면 안 된다.

```
┌─ 공개 층 ──────────────────────────────────────────┐
│  GitHub Pages                                       │
│    *.html          도구 (자료 없는 껍데기)             │
│    data/*.json     대학이 공개한 자료                  │
│    ../College/     전문대 자료 (같은 계정, 같은 출처)    │
│                                                     │
│  누구나 볼 수 있다. 학생 개인은 하나도 없다.             │
└─────────────────────────────────────────────────────┘
                          ↕  브라우저 안에서만 만난다
┌─ 개인 층 ──────────────────────────────────────────┐
│  교사 구글시트 (Apps Script 웹앱)                     │
│    학생 이름 · 내신 · 수능 · 지원 · 메모 · 결과          │
│                                                     │
│  잠금은 **배포 주소**다. 주소를 아는 사람만 들어온다.     │
└─────────────────────────────────────────────────────┘
```

**결합은 브라우저 안에서만 일어난다.** 학생 자료가 공개 층으로 가는 요청도, 공개 층이
학생 식별자를 받는 요청도 만들지 않는다.

계정으로 잠그지 않은 까닭 — 선생님들이 학교 컴퓨터·집·휴대폰을 오가며 쓴다.
계정으로 고정하면 정작 써야 할 사람이 막히는 일이 더 잦았다. 계정 명단으로 잠그고
싶으면 시트 `설정` 탭에 이메일을 적으면 그 순간부터 그 명단만 들어온다.

`.gitignore` 가 `*.xlsx` 를 막는다. 커밋 전에 `git status` 를 본다.

---

### 즐겨찾기 원본은 딴 파일에 둘 수 있다

기본은 상담 기록과 **같은 시트**의 원본 탭이다. `설정` 탭 B2 에 다른 스프레드시트
주소를 적으면 거기서 읽는다.

즐겨찾기는 9월 내내 바뀐다 — 학생이 지원을 더하고 빼고, 대교협에서 새로 내려받는다.
상담 기록이 여덟 탭이나 같이 든 파일에서 탭을 지우고 붙여 넣는 것은 위험하다.
원본만 딴 파일에 두면 그 파일만 갈아끼우면 되고 기록이 든 시트는 안 건드린다.

**못 열면 조용히 이 시트로 돌아가지 않는다.** 그러면 지난주 자료를 이번 주 자료로
읽는다 — 제일 조용히 틀리는 길이다. 화면에 붉게 알린다.

어느 쪽이든 순위·메모·결과는 **안정키**로 붙어 있어서 원본이 통째로 바뀌어도 같은
지원에 그대로 다시 붙는다.

---

## 2. 도구 여섯

| 파일 | 무엇 | 자료 | 크기 |
|---|---|---|---|
| `index.html` | 수시입결검색기 (2022~2026) | 입결 **내장** | 8.6 MB |
| `planner.html` | 2027 전형설계 검색기 | 모집요강 **내장** | 5.2 MB |
| `eligibility.html` | 지원자격·중복지원 조회 | 지원자격 **내장** | 4.8 MB |
| `board.html` + `board/` | **수시 상담 보드** (교사) | `fetch` + 시트 | 6.7 KB |
| `student.html` + `board/` | 학생용 내 지원 현황 | `fetch` + 시트 | 1.8 KB |
| `counsel.html` | 내신 중심 진학상담 | 시트 | 91 KB |
| `peer.html` | 또래비교·추천 | 로컬 빌드 주입 | 44 KB |

### 왜 어떤 건 내장이고 어떤 건 fetch 인가

**내장(single-file)** — `index` · `planner` · `eligibility`.
선생님이 파일 하나를 받아 USB 로 옮기거나 `file://` 로 열어도 그대로 돌아가야 한다.
자료가 통째로 `<script id="raw-data">` 안에 들어간다. 크기가 5~8 MB 로 커지지만,
「열면 된다」가 이 도구들의 요건이다.

**fetch** — `board` · `student`.
자료 넉 벌(입결·모집요강·전형일정·전문대)을 붙이는데, 다 내장하면 15 MB 가 넘고
자료를 고칠 때마다 HTML 을 다시 만들어야 한다. 그래서 `data/*.json` 을 따로 두고 받아
온다. 대신 `file://` 로는 안 열린다 — Pages 로만 쓴다.

`index.html` 은 두 길을 다 둔다. `data/ipgyeol.json` 을 정본으로 두고
`scripts/sync_index_data.py` 가 그걸 HTML 안에 밀어 넣는다. 자료는 한 군데서 고치고,
파일은 혼자서도 열린다.

---

## 3. 자료가 흐르는 길

```
  대교협·컨설팅 엑셀 ─┐
  수박먹고 PDF ───────┤
  College 저장소 ─────┘
          │
          │  scripts/*.py   (사람이 손으로 돌린다. CI 없음)
          ↓
      data/*.json          ← 정본. 공개.
          │
          ├── <script> 내장 ──→ index · planner · eligibility   (xref.json 은 여기만)
          └── fetch ──────────→ board · student
                                입결 · 모집요강 · 전형일정 · 전문대(College) 넷
                                    ↑
                                    │  JSONP
                                    │
                          Apps Script 웹앱
                                    ↑
                          교사 구글시트 (학생 자료)
```

### `data/` 넉 벌

| 파일 | 무엇 | 만드는 것 | 크기 |
|---|---|---|---|
| `ipgyeol.json` | 2022~2026 수시 입결 75,494행 | `build_ipgyeol.py` | 8.9 MB |
| `track_alias.json` | 전형명 별칭표 484건 | `build_ipgyeol.py` | 0.02 MB |
| `mojip2027.json` | 2027 모집요강 16,873행 · 223개교 | `parse_2027.py` | 4.7 MB |
| `schedule2027.json` | 전형일정 — 원서마감·발표·고사일 | `build_schedule.py` | 0.2 MB |
| `xref.json` | 학과 이름 맞춤표 | `build_xref.py` | 0.4 MB |

`xref.json` 은 **보드가 안 받는다.** `planner.html` 이 내장해 쓰는 것이고, 보드는
`match.js` 가 그때그때 이름을 맞춘다(§5). 보드가 받는 것은 위 셋에 College 저장소의
`departments.json` 을 더한 넷이다.

`mojip2027.json` 과 `schedule2027.json` 은 **문자열 사전 방식**이다. 같은 글이 수천 번
되풀이되므로(`수능최저`·`지원자격` 원문) 문자열을 `strings` 배열에 한 번만 두고
행에는 번호를 넣는다. 읽는 쪽은 `textCols` 에 든 열만 사전에서 꺼낸다.

### 스크립트 여섯

전부 **손으로 돌린다.** 자동화하지 않은 까닭 — 원본이 해마다 모양이 바뀌는 엑셀·PDF라,
돌릴 때마다 사람이 결과 숫자를 봐야 한다. 그래서 모든 스크립트가 마지막에 **무엇이
얼마나 붙었는지 재서 찍는다.**

| 스크립트 | 하는 일 |
|---|---|
| `parse_2027.py` | 모집요강 엑셀 → `mojip2027.json` |
| `merge_ipgyeol.py` | 갈라져 있던 입결 두 벌을 하나로 |
| `build_schedule.py` | 전형일정 PDF 다섯 부 → `schedule2027.json` |
| `build_xref.py` | 즐겨찾기·입결·모집요강 학과명 맞춤표 |
| `build_planner.py` | `planner.html` 에 모집요강 밀어 넣기 |
| `sync_index_data.py` | `index.html` 에 입결 밀어 넣기 |

---

## 4. 상담 보드 (`board/`)

가장 큰 부분이라 따로 본다. 파트 계약은 [`board/CONTRACT.md`](board/CONTRACT.md).

```
board.html ─┬─ api.js         JSONP 로 Apps Script 부르기
            ├─ store.js       자료 적재 · 상태 · 쓰기
            ├─ match.js       이름 맞추기 (순수)
            ├─ stats.js       결과 통계 (순수)
            ├─ confidence.js  얼마나 믿을 수 있나 (순수)
            │
            ├─ board.js       6칸 보드
            ├─ card.js        카드 상세
            ├─ schedule.js    일정판 · 겹침
            ├─ links.js       학생 링크
            ├─ verify.js      점검 (학과 잇기)
            └─ export.js      진학 대장 · 결과 보고서

student.html ── student.js    학생 화면 (같은 match.js · confidence.js 를 쓴다)
```

### 누가 무엇을 부를 수 있나 — 두 관점으로 훑다가 나온 것

**1. `students` 가 학생 경로에 먹혔다.**

```js
if (action.indexOf('student') === 0) return studentAction_(action, p);
```

`'students'.indexOf('student')` 는 0 이다. 교사 화면의 자료 적재인 `students` 가
여기 걸려서 토큰을 요구받았고, 늘 「만료되었거나 잘못된 주소입니다」로 떨어졌다.
`case 'students': return loadAll_(me)` 는 **닿을 수 없는 코드**였다.
보드를 실제 시트에 붙이면 자료가 통째로 안 올라온다 — 보기용 자료(`?demo=1`)는
서버를 안 불러서 여태 안 드러났다. 이름을 또박또박 적은 표(`STUDENT_ACTION`)로 가른다.

**2. 학생이 전교생 자료를 가져갈 수 있었다.**

학생 링크에는 배포 주소가 `?api=` 로 들어 있다. 학생 링크를 열려면 「액세스: 모든
사용자」로 배포해야 하는데, 그러면 구글이 접속자 계정을 안 알려 줘서 `access_()` 가
아무나 통과시킨다. 학생이 제 링크에서 주소만 떼어 `?action=students` 를 부르면

```
학생: 3101 김가나 (내신 …) / 3102 이다라 …
지원: 건국대학교(서울) 영어영문학과 내등급 3.17 …
메모: [비공개] 최저 못 맞출 것 같음. 하향 권유 필요.
```

가 그대로 나왔다. **실제로 돌려 본 결과다**(`board/auth.test.mjs`).
「보이지 않는 것 — 다른 학생, 교사 비공개 메모」라고 적어 둔 것과 정반대다.

시트 `설정` 탭 C2 에 **교사 열쇠**를 두고 보드만 함께 보낸다. 학생 링크에는 안
들어간다. 열쇠를 안 적어 두면 **막지 않고 알린다** — 옛 시트를 쓰던 분이 갑자기 못
들어오는 편이 더 나쁘다. 대신 보드가 붉은 줄로 「지금 누구나 볼 수 있습니다」라고
계속 말한다.

기본 열쇠(`84348434`)를 코드에 박아 두어 설치할 때 손댈 것을 없앴다. C2 에 다른
글자를 적으면 그쪽이 이긴다.

**이 열쇠는 암호가 아니라 빗장이다.** 기본값은 이 저장소에 적혀 있으니 찾아본
사람은 읽을 수 있다. 막는 것은 「학생이 제 링크에서 주소만 떼어 `?action=students`
를 불러 보는 것」까지다. 진짜로 잠그려면 C2 에 저장소에 없는 글자를 적어야 하고,
그동안 보드가 「기본 열쇠를 쓰는 중」이라고 한 줄로 알린다.

더 제대로 하려면 교사용과 학생용을 다른 배포로 나눠야 하는데 설치가 두 배가 된다 —
3학년실 규모에서는 빗장으로 충분하다고 봤다.

**같은 함정에 하루에 두 번 빠졌다.** 서버에서 `action.indexOf('student') === 0` 이
`students` 를 삼키는 것을 고쳐 놓고, 열쇠를 붙이는 곳에 `startsWith('student')` 를
새로 심었다. 교사 자료 적재에 열쇠가 안 붙어 「열쇠가 맞지 않습니다」가 됐다.
이제 양쪽 다 **또박또박 적은 목록**(`STUDENT_ACTION`)을 쓰고, 두 목록과 기본 열쇠가
어긋나면 `board/auth.test.mjs` 가 잡는다.

### 대교협 파일은 모양이 둘이다 — 머리글 줄을 찾는다

해마다·메뉴마다 다른 파일이 온다.

```
즐겨찾기                        관심대학 리스트
1행  학년 반 번호 성명 …         1행  (비어 있음)
2행  100  80  (내신 아래 머리글)  2행  관심대학 리스트      ← 제목
3행~ 자료                        3행  (비어 있음)
                                 4행  No 학년 반 번호 이름 …  ← 머리글
                                 5행~ 자료
```

머리글을 첫 줄로 못 박아 두면 관심대학 리스트는 **모든 칸이 미인식**이 되어 통째로
빈다. 그래서 줄 번호를 정하지 않고 **아는 이름이 가장 많이 맞는 줄**을 머리글로
본다(`readHeader_`). 「이 파일은 A형」 하고 두 벌을 따로 짜는 것보다 낫다 — 내년에
또 다른 모양이 와도 이름만 알아보면 그대로 걸린다.

곁들여 필요한 것 셋.

- **칸 이름 안의 줄바꿈을 지우고 견준다**(`head_`). 「모집↵인원」·「수능↵최저↵유무」로 온다.
- **머리글이 한 줄뿐이면 빈 칸에 앞 이름을 물려주지 않는다.** 합쳐진 머리글은 아래
  머리글 줄이 있을 때만 생긴다. 관심대학 리스트에는 실기일자와 논술일자 사이에
  이름 없는 빈 열이 하나 있어서, 물려주면 그게 「실기일자」가 된다.
- **0 은 값이 아니라 「아직 안 적음」이다**(`score_`). 내등급·내점수를 안 넣은 줄이
  빈칸이 아니라 0 으로 온다. 그대로 두면 카드에 「내 환산 등급 0.00」이 떠서
  1등급보다 좋은 성적으로 읽힌다. 실제 파일 502줄 가운데 29.3% 가 그랬다.

「수능 최저 유무」는 Y/N 만 준다. **N 을 그냥 기준 글 칸에 넣으면 안 된다** — 글자가
있으니 참이 되어 최저가 없는 전형에 「최저 있음」이 붙는다. 있다/없다/모른다 셋으로
가르고(`minReq`), 카드도 「없습니다」와 「모르겠습니다」를 다르게 적는다.

**모양이 바뀌면 안정키도 바뀐다.** 두 파일이 같은 전형을 다르게 적어서
(「종합(KU자기추천)」 ↔ 「학생부종합(KU자기추천전형)」) 순위·메모·결과가 떨어진다.
파서가 고칠 수 있는 문제가 아니라 — 어느 쪽이 옳은지 자료가 말해 주지 않는다 —
「학년 중간에 바꾸지 않는다」로 푼다. `board/parse.test.mjs` 가 이 사실을 못 박아 두고,
`board/SETUP.md` 가 선생님께 알린다.

두 모양의 **지어낸 표본**을 `board/fixtures/` 에 두고 시험이 늘 둘 다 읽는다.
실제 시트는 실명이 들어 있어 커밋하지 않는다 — 필요하면 경로로 넘긴다.

#### 시나리오를 돌려 보고 잡은 것 넷

모양을 둘로 늘린 뒤 「올 법한 이상한 자료」를 넣어 봤다. 넷이 걸렸고 셋은
**이번에 생긴 것이 아니라 원래 있던 것**이다.

- **시트는 날짜 칸을 `Date` 객체로 준다.** `getValues()` 는 날짜 서식이 걸린 칸을
  문자열이 아니라 Date 로 돌려주는데, 그냥 `String()` 하면
  「Sun Nov 29 2026 00:00:00 GMT+0900 …」이 되어 날짜 파서가 못 읽는다.
  면접일이 통째로 사라지는 자리다. `txt_` 가 `YYYY-MM-DD` 로 편다
  (`toISOString()` 은 UTC 로 옮기느라 한국 시간 0시가 전날이 된다).
- **`dept` 가 일정으로 샜다.** 일정 칸을 `d면접`·`d실기` 로 이름 붙이고
  「`d` 로 시작하면 일정」으로 골랐는데, `dept`(모집단위)가 걸렸다. 학과 이름을
  날짜로 읽으려다 실패해서 지원마다 `unknown['일정/ept'] = '영어영문학과'` 가
  붙어 있었다. 미인식 칸은 열 단위로만 세는 탓에 요약에 안 나와 오래 안 보였다.
  접두사를 `날짜:` 로 바꿨다.
- **자료 줄을 첫 칸으로 골랐다.** 관심대학 리스트의 첫 칸은 줄 번호(`No`)라
  우연히 숫자였을 뿐이다. 그 칸이 비거나 「A-1」 같은 글자면 그 줄이 통째로
  사라진다. 이제 `학년` 칸을 본다.
- **「-」를 못 읽은 날짜로 셌다.** 「-」는 「아직 없다」는 표시지 읽기 실패가
  아니다. 실제 파일 502줄이 전부 그래서, 지원마다 「알아보지 못한 일정: -」이
  붙어 있었다.

곁들여 **엉뚱한 파일을 가리켰을 때** 조용히 「0명」이 되지 않게 했다. 알아본 칸이
넷보다 적으면 읽기를 포기하고 사유를 돌려주고, 보드가 붉게 띄운다. 학년·반·번호·
이름만 알아봐도 넷이라 진짜 즐겨찾기는 이 문턱에 걸릴 수 없다.

### 버린 줄은 반드시 말한다

`parseFavorites_` 는 대학이나 학과 칸이 빈 줄을 지원으로 안 센다. 그런데 **학생은
그 전에 이미 만들어진다.** 그래서 그런 줄뿐인 학생은 명단에는 올라가 있고 보드만
텅 비었다 — 원래 지원이 없는 건지 자료가 샌 건지 알 길이 없다.

여태 `skipped` 를 세기만 하고 **아무 데도 안 냈다.** 이제 셋을 낸다.

- 몇 줄을 왜 버렸는지 (`dropped: [{hak, n, why, row}]`)
- 보드 위에 붉은 줄로 「대학이나 학과 칸이 비어 N줄을 못 읽었습니다 — 3199(3줄)」
- 지원이 0건인 학생을 열면 빈 화면 대신 그 사유

### 카드끼리 맞바꾸기는 **있었는데 안 보였다**

차 있는 순위를 고르면 예전부터 둘이 자리를 바꿨다(`move` 의 `pushed`). 그런데
목록에는 「2순위」라고만 적혀 있어서 거기 있던 카드가 밀려나는 것처럼 보였다.
차 있는 칸은 「2순위 ⇄ 건국대 영어영문학과」로 적는다 — 기능을 더한 게 아니라
있는 것을 보이게 한 것이다.

### 빈 6칸은 **늘 그린다** — 놓을 자리가 먼저다

한때는 한 칸도 안 정했으면 격자를 감추고 두 줄로 줄였다. 화면을 아낀다는
뜻이었는데, 정작 상담을 시작할 때 **채울 자리가 안 보였다.** 여섯 칸은 수시가
여섯 장이라는 사실 그 자체고, 빈 칸 여섯은 「아직 아무것도 안 정했다」를 한눈에
말한다. 이제 늘 그리되, 한 칸도 안 찼을 때는 칸 키를 128px → 56px 로 낮춰
(`.slots.thin`) 아래 지원 목록을 화면 밖으로 밀지 않는다.

### 카드가 자리를 옮기는 것이 **보여야 한다** (FLIP)

순위를 바꾸면 화면을 새로 그린다. 새로 그리기만 하면 카드가 순간이동해서, 두 장이
맞바뀐 건지 한 장이 사라지고 다른 게 나타난 건지 눈이 못 따라간다.

거기에 예전에는 `place` 를 **두 번** 불렀다. 밀려난 카드가 먼저 움직이고, 서버
왕복을 기다린 뒤에 누른 카드가 움직였다 — 그 사이에 반쯤 바뀐 화면이 보였고
화면을 네 번 다시 그렸다. 「부드럽지 않다」의 정체가 이것이다.

- `store.placeMany(moves)` — 자리를 **한꺼번에** 바꾸고 `emit` 은 한 번,
  쓰기는 `Promise.all`, 하나라도 실패하면 **전부** 되돌린다.
- `board.js` 의 `cardRects()` / `glide()` — 그리기 **전에** 카드 자리를 재 두고,
  그린 **뒤에** 그 차이만큼 `transform` 으로 되돌렸다가 제자리로 미끄러뜨린다.
  200ms · `cubic-bezier(0.2,0,0,1)` · `transform` 만. 1px 미만은 안 움직인 것으로 친다.
- `prefers-reduced-motion: reduce` 면 아예 안 움직이고, `el.animate` 가 없으면
  조용히 건너뛴다. 끌어다 놓기도 같은 `move()` 를 지나므로 함께 미끄러진다.

카드마다 `data-id` 를 단다 — 다시 그린 뒤에 「이게 아까 그 카드」임을 짚는 유일한
끈이다. 6칸의 카드(`.card`)와 목록의 줄(`.row`) 둘 다 단다.

### 카드 한 장이 터져도 **보드는 살아 있어야 한다**

숫자 묶음이 통째로 빌 수 있다. 입결에는 이어졌는데 70%컷도 경쟁률도 안 적힌
전형이 있다(올해 새로 묶인 광역모집·자유전공에서 나온다). `figures()` 가 그때
`null` 을 받아 놓고 곧장 `title` 을 달다 터졌고, **`render()` 가 통째로 멈춰서
명단에는 이름이 있는데 보드만 텅 빈 학생**이 생겼다. 실제 자료 80명 중 **15명**이
그랬다 — 상담 도구에서 가장 나쁜 실패다.

둘 다 고쳤다.

1. 빈 묶음을 그대로 다룬다 (`if (head)`).
2. 카드 한 장을 그리는 일을 `cardRow()` 로 떼어 내고 `try` 로 감쌌다. 못 그린
   카드는 「이 카드를 그리지 못했습니다」로 남기고 **나머지는 그대로 낸다.**
   한 장의 버그가 다시는 한 학생을 통째로 지우지 못한다.

### 연결을 다시 쟀다 — 네 군데가 새고 있었다

김담유 카드에서 시작해 실제 지원 502건과 2027 모집요강 16,853가지를 전부 돌려 봤다.
**네 가지가 서로 다른 이유로 틀리고 있었다.**

**하나. 괄호 안 세부전공을 지운 대가.** `normDept` 는 괄호를 통째로 턴다.
「경영학과(주간)」의 군더더기를 지우려던 것인데 진짜로 학과를 가르는 괄호까지 지워졌다.

| 즐겨찾기 | 붙어 있던 숫자 | 맞는 숫자 |
|---|---|---|
| 국민대 미래융합전공**(A)** | 2.12 · 4.7:1 ← (B) | **2.08 · 8.7:1** |
| 국민대 자유전공**(A)** | 2.33 · 4.4:1 ← (B) | **2.16 · 7.6:1** |
| 한양대(E) LIONS**(인문사회계열)** | 2.56 · 6.8:1 ← 전계열 | **2.82 · 8.7:1** |
| 전남대(여) 국제학부**(일본학전공)** | 5.69 · 1.9:1 ← 중국학 | **5.23 · 11.2:1** |
| 경북대 전자공학부 | 3.42 ← 인공지능전공 | **2.20** |

같은 대학·연도·전형 안에서 **숫자까지 다른 채 한 키로 뭉개진 자리가 961곳**이다
(숫자가 같아 무해한 1,565곳은 뺀 수다). 그중 하나가 아무 근거 없이 뽑히고 있었다.
이제 `narrowDept` 가 **괄호까지 그대로 같은 줄이 있으면 그것만** 보고, 없으면
아무것도 안 고르고 **어떤 갈래가 있었는지 적는다**(을지대 간호학과 성남/의정부처럼).
해마다 따로 잰다 — 국민대는 2025에 (인문)·(자연), 2026에 (A)·(B)로 갈라 놓았다.

**둘. 「작년에 안 뽑았다」를 「올해 신설」로 읽었다.** 모집요강은 작년까지만 안다.
단국대(죽전) 정치외교 「교과(지역균형)」은 2022~2025 내내 뽑다가 2026 한 해를
건너뛰었을 뿐인데 「올해 신설이라 작년 값이 없습니다」가 떴고, 네 해치 컷을 갖고도
카드가 텅 비었다. 화면에는 「신설」과 「내가 넣은 전형(2022~2025)」이 나란히 떴다.
이제 **입결에 같은 이름이 이미 있으면(`exact`) 신설이 아니다.** 모집요강 16,853가지
가운데 **266건**의 「신설」이 취소됐다. 반대로 건국대(글로컬) 「지역의사제(광역권)」처럼
이름만 닮은 것(`near`)은 여전히 신설로 둔다 — 그 572건이 도로 남의 컷을 달면 안 된다.

**셋. 쪼개진 전형을 후보에서 빼다가 엉뚱한 하나를 이었다.** 이름이 바뀐 전형을
잇는 규칙은 「앞도 뒤도 후보가 하나뿐일 때만」인데, 쪼개진 짝(`교과(학생부교과1)`·`2`)을
**세기 전에** 빼 버려서 남은 `교과(지역인재)` 가 혼자가 됐다. 한서대 항공관광학과
두 학생 카드에 4.3 대신 5.0·경쟁률 2.59가 붙어 있었다. 이제 **후보로는 세고 잇지만
않는다.** (반대로 통째로 막았더니 충남대 소비자 `종합Ⅰ` 이 `종합Ⅰ(서류)` 에 붙었다 —
두 방향 다 재고 나서 잡은 자리다.)

**넷. 면접형에 서류형 컷이 붙었다.** 이 가림은 `pickHeir`(쪼개진 전형)에만 있었다.
이제 **모든 갈래**에 앞서 적용한다 — 이름에 「면접」만 있으면 면접형, 「서류」만 있으면
서류형이고, 어긋나는 묶음은 애초에 후보에서 뺀다. 가톨릭대(성심)
「잠재능력우수자면접」에 붙어 있던 「잠재능력-서류」 2.91이 사라졌다.
지운 자리를 **옆 전형으로 메우지도 않는다** — 명지대 「명지인재면접」이 서류형을 지운
뒤 「종합(크리스천리더)」에 붙던 것을 막았다.

### 이름이 닮으면 잇는다 — `sim` 갈래

`near` 는 한쪽이 다른 쪽을 **통째로 감쌀 때만** 잡는다. 가운데 한두 글자가 다르면
다 놓쳐서, 맞는 줄을 유형(`cat`)으로 집어 놓고도 카드에는 「전형을 이름으로 맞추지
못했습니다」라는 붉은 줄이 붙었다. **선생님이 맞은 것을 틀렸다고 읽는 자리다.**

    고려대   교과:학교추천        ↔  교과(학교장추천)     0.80   「장」 한 글자
    광운대   종합광운참빛1면접형   ↔  종합(광운참빛-면접)   0.81
    원광대   교과지역교과호남권    ↔  교과(지역-호남)      0.77
    가천대   종합가천바람개비      ↔  종합(바람개비)       0.74
    전북대   교과지역1유형        ↔  교과(지역1호남)      0.69

**문턱 0.65 는 재서 잡았다.** 실제 지원 502건에서 여기까지 내려오는 짝의 닮은 정도에
0.63과 0.69 사이가 갈림목이다. 0.69 위는 눈으로 봐도 같은 전형이고, 0.63 아래는
명지대 `명지인재면접`↔`명지인재서류`(0.63) · 성균관대 `서류형기회균형`↔`융합형`(0.44)
처럼 **다른 전형**이다. 셋을 다 만족할 때만 고른다 — 유형이 같고, 0.65 이상이
하나뿐이며 2등과 0.1 이상 벌어지고, 면접·서류 표시가 어긋나지 않을 것.

즐겨찾기가 쌍점으로 적는 대학(고려대 「학생부교과:학교추천」)이 있어 `normType` 이
쌍점도 턴다. 입결·모집요강 쪽 전형 이름을 전부 넣어 보면 구두점이 **하나도 안 남으므로**
여기서 더 지워도 자료 쪽에서 새로 부딪힐 이름이 없다.

**전체 성적표** (모집요강 16,853가지)

| | 전 | 후 |
|---|---|---|
| 이름으로 맞춤 (exact·near·sim) | 11,930 | **12,220** |
| 유형만 맞춤 (cat) | 552 | **445** |
| 못 맞춤 (none) | 1,956 | **1,765** |

컷이 새로 생긴 것 412 · 사라진 것 18 · 바뀐 것 25. **바뀐 25건은 하나도 빠짐없이
다른 갈래의 숫자를 제 갈래 숫자로 되돌린 것**이고, 사라진 18건은 위의 가톨릭대·을지대처럼
애초에 틀린 연결이거나 갈래를 못 가려 물어봐야 하는 것이다.

### 학과 이름을 못 찾으면 두 번 더 두드린다

즐겨찾기와 입결이 같은 학과를 **다른 자리에서 자른다.**

| 즐겨찾기 | 입결 |
|---|---|
| 조선대 국어국문학부**(국어국문학전공)** | 국어국문학전공 |
| 상명대 인문콘텐츠학부 **문헌정보학전공** | 문헌정보학전공 |
| 동아대 정치사회학부**(사회학전공)** | 사회학전공 |
| 조선대 약학과**(6년제)** | 약학과 |

앞은 학부를 앞에 달고 전공을 뒤에 단 것이고 뒤는 수업 연한이다. 둘 다 같은 학과인데
글자로는 안 겹친다. 못 찾았을 때만 「학부(전공)」의 **전공 쪽만**, 그리고 「(N년제)」를
뗀 이름으로 다시 찾는다. **줄여서 찾은 것이 한 벌일 때만 쓰고**, 어떤 이름으로 찾았는지
카드에 적는다. 실제 지원에서 6건이 새로 붙었고 잃은 것은 없다.

이것은 **이름이 바뀐 학과를 잇는 것과 다르다.** 그쪽은 여전히 자동으로 안 잇는다
(`predecessor` — 이어 붙인 경계에서 컷이 기준선을 넘는 비율이 기대치의 세 배다).
여기는 같은 이름을 다르게 자른 것을 도로 붙이는 것뿐이다.

### 못 이으면 **신설이라고 말한다**

「입결 자료에 없습니다」는 선생님이 자료가 빠진 줄 알고 찾아 나서게 한다. 실제로는
올해 개편이 잦아서 — 광역모집·자유전공·AI 붙은 학과가 해마다 새로 생긴다 — 찾을 것이
애초에 없는 경우가 많다.

입결을 못 붙였을 때는 **학과 단위로** 묻는다. 모집요강이 이 학과로 내놓은 전형이
하나도 빠짐없이 작년 모집인원·경쟁률이 비었으면, 작년에 이 학과로 아무도 안 뽑은 것이다
(`deptIsNew`). 전형 이름이 맞았는지는 안 따진다 — 학과 단위 물음이라 상관이 없다.

점검 화면은 그렇게 판정된 학과를 **접어 둔다.** 실제 자료에서 못 붙인 24종 가운데
8종이 여기 걸려, 선생님이 실제로 손댈 것은 16종으로 줄었다. 지우지는 않는다 —
판정이 틀렸다 싶으면 접힌 자리에서 그대로 이을 수 있다.

### 손으로 이어 놓은 학과가 목록에서 안 사라지던 것

학과를 **여럿** 이으면(자유전공처럼 작년 한 학과에 대응하지 않는 자리) 이름을 바꾸지
않으므로 `makeLink` 는 여전히 「못 찾았다」를 준다. 그런데 점검 화면은 그 값 하나로
「아직 못 붙인 것」을 세서, **이어 놓은 학과가 목록에 영영 남았다.** 이어도 안 사라지니
같은 것을 몇 번씩 다시 잇게 된다. 참고선이 그려졌으면 이건 붙은 것이다 — 다만 제 학과의
값이 아니라 참고 범위라 `loose` 다. 모집요강이 주는 「묶이기 전 참고」와 같은 등급이다.

### 면접 날짜를 적을 칸이 **한 번도 안 나오고 있었다**

「확정일은 통보받은 학생이 넣는 게 가장 정확하다」고 만들어 놓고, 정작 넣을 자리가
화면에 안 떴다. 조건이 **「이미 날짜가 있을 때」**였기 때문이다 — 즐겨찾기가 면접
기간을 주었거나 학생이 이미 적었을 때만 줄이 섰다.

그런데 실제 관심대학 리스트에는 **일정 칸이 통째로 비어 있다. 502건 중 0건**이다.
서울시립대 종합처럼 대놓고 2단계인 카드에서도 날짜 칸이 안 나왔다.

이제 **볼 근거가 있으면 빈 칸이라도 세운다.**

| | 근거 |
|---|---|
| 면접 | 모집요강이 단계별전형이라고 말할 때 (전형단계 ≥ 2) |
| 논술 | 전형 유형이 논술 |
| 실기 | 전형 유형이 실기 |
| 적성 | 즐겨찾기가 날짜를 줄 때만 |

실제 502건으로 재 보니 모집요강이 전형단계를 아는 것이 **87.6%** 이고 그중 단계별이
71건이다. 나머지는 일괄전형이라 면접이 없는 게 맞아서 칸도 안 선다 — 근거 없이 다
세우면 면접 없는 카드마다 빈 날짜 칸이 붙어 시끄러워진다.

**근거가 없어도 길은 열어 둔다.** 전형단계를 모르는 지원이 12% 있고, 알아도 틀릴 수
있다. 면접 통보를 받았는데 적을 자리가 없으면 그 학생에게는 이 기능이 없는 것과 같다.
칸이 하나도 안 선 카드에는 「면접·실기 날짜 적기」 한 줄이 접힌 채로 붙는다 — 펴면
종류를 고르고 날짜를 넣는다. 넣고 나면 그 종류의 진짜 줄로 바뀐다.

### 순위를 학생도 바꾼다 — 그리고 자리다툼을 서버가 막는다

어디를 몇 순위로 넣을지는 원래 학생이 정하는 것이다. 여태 상담 때 담임만 바꿀 수
있었는데, 집에서 밤에 마음이 바뀌는 쪽은 학생이다. 학생 화면 맨 위에 **빈 칸 여섯이
먼저** 서고(휴대폰에서 두 줄에 셋씩), 아래 카드마다 순위 고르개가 붙는다. 차 있는
순위를 고르면 「2순위 ⇄ 국어국문학과」처럼 무엇과 맞바꾸는지 미리 적어 둔다.

**둘이 같은 6칸을 동시에 만지면 나눠 쓰기로는 못 막는다.**

    학생   2순위 카드를 1순위로        →  (쓰기1) 그 카드 rank=1
    담임   같은 순간 다른 카드를 1순위로  →  (쓰기1) 그 카드 rank=1
    학생   (쓰기2) 밀려난 카드 rank=2
    담임   (쓰기2) 밀려난 카드 rank=2   ← 두 카드가 나란히 1순위

그래서 **맞바꾸기를 통째로 서버가 한다**(`setRank_`). 잠금 안에서 지금 배치를 읽고,
밀려날 카드를 찾고, 둘을 한꺼번에 쓴다. 중간이 없으니 끼어들 자리도 없다. 교사 보드의
`placeMany` 도 이제 이 한 번의 호출을 쓴다.

거기에 **한 발 늦은 화면을 되돌린다.** 화면은 마지막으로 본 배치의 시각(`seen`)을 같이
보내고, 그 사이에 저쪽이 건드렸으면 서버가 쓰지 않고 그렇다고 말한다. 학생 화면은
그 답을 받으면 **스스로 다시 불러온다** — 새로고침을 시키면 학생은 자기가 무엇을
놓쳤는지 모른 채 같은 것을 다시 누른다.

순위는 확인 대기로 두지 않는다. 날짜·접수번호·결과는 잘못 적으면 판단이 흔들려서
담임이 확인하지만, 순위는 틀려도 다시 바꾸면 그만이다. 누가 바꿨는지는 `by` 에
「3201 학생」으로 남는다.

### 두 화면이 같은 날짜를 말하게 한다 — 전형일정표는 「예정」이다

담유의 숭실대 면접형에서 드러났다. 교사 달력에는 11/27 면접이 「가는 날」로 꽉 채워
칠해져 있는데, 학생 카드는 같은 지원에 「아직 날짜가 없습니다」라고 했다. 원인이 둘이었다.

**하나 — 학생 화면이 전형일정표(schedule2027.json)를 아예 안 받았다.** 교사 쪽
`store.dateOf` 는 「내가 넣은 값 → 즐겨찾기 확정일 → 전형일정표(이름까지 맞을 때만) →
즐겨찾기 기간」 차례인데 학생 쪽 `dateOf` 는 앞 둘만 봤다. 이제 같은 차례를 탄다.
실제 502건에서 이름까지 맞는 것이 36건, 유형만 맞는 것이 121건인데 **유형만 맞는
것은 여전히 안 쓴다** — 원광대 종합 지원에 의예과들 면접일이 붙는 꼴이 그 121건에
실제로 들어 있다. 카드 상세에서 「(참고) 이 대학 같은 유형 고사일」로만 보인다.

**둘 — 일정표에서 온 날짜가 사람이 잡은 날처럼 보였다.** 사실이긴 하다(그 전형의
고사일이다). 그러나 이 학생의 확정이 아니다. 그래서:

- 달력에서 꽉 채운 칠은 **사람이 넣거나 확인한 날**(confirmed·즐겨찾기)에만.
  일정표에서 온 것과 확인 대기는 점선이고, 제목에 출처가 붙는다.
- 겹침도 마찬가지 — 일정표 날짜로는 「겹침」을 확정하지 않고 「겹칠 수 있음」까지만.
  교사 일정판과 학생 화면이 같은 규칙을 쓴다.
- 학생 카드 꼬리표(「면접 11/27(금)」)도 확정만 꽉 채운 칠, 나머지는 점선.

**파싱한 날짜는 어디서든 고칠 수 있다.** 일정표·즐겨찾기 값은 대학 전체를 두고 한
말이라 실제 배정일과 다를 수 있는데, 여태 고칠 자리가 없었다. 학생 카드는 입력칸에
그 날짜를 **미리 채워** 두고(맞으면 저장 한 번, 다르면 고쳐서 저장), 교사는 일정
탭의 각 줄과 **보드 카드 상세**에 「날짜 넣기 · 고치기」가 붙었다. 사람이 넣은 값이
언제나 파싱값을 덮는다.

**교사 일정 탭에도 학생 화면과 같은 구멍이 있었다.** 날짜가 하나도 없으면 탭이
통째로 빈말 한 줄이었고, 「면접 준비」 판과 날짜 넣는 칸까지 같이 사라졌다. 이제
학생을 골랐으면 날짜가 없어도 「면접 준비」를 그린다 — 단계별전형이면 빈 면접일
칸이 서고, 근거가 없는 지원은 「여기 없는 지원에 날짜 적기」 접힌 줄로 닿는다.

### 확인까지 끝난 면접일이 사라진 까닭 — 시트가 날짜를 Date 로 되돌려 준다

학생이 면접일을 넣고 담임이 「맞습니다」까지 눌렀는데 날짜가 화면에서 사라졌다.
저장은 멀쩡했다 — **읽어 오는 길**이 문제였다.

학생이 「2026-11-28」을 넣으면 구글 시트가 그 칸을 날짜형으로 바꾼다. `rows_()` 가
읽으면 글자가 아니라 Date 객체고, JSON 으로 나가면서 UTC 로 적혀
`2026-11-27T15:00:00.000Z` — **하루 전 날짜에 시각까지 붙은 딴 값**이 된다.
달력은 「from ≤ 날짜 ≤ to」 글자 비교라 이 값과는 어떤 날도 안 맞고, 그래서
넣고 확인까지 끝난 날짜가 달력·카드에서 통째로 사라졌다.
`parseFavorites_` 쪽에서 이미 한 번 잡았던 함정(`txt_`)이 읽기 경로에 그대로 있었다.

- 서버 `cell_` — 시트에서 읽는 모든 행의 Date 를 KST 글자로 씻는다. 자정이면
  날짜만(`2026-11-28`), 시각이 있으면 시각까지(`2026-09-02T10:11:00+09:00`) —
  배치의 `at` 은 순위 충돌 검사(`seen`)가 시각을 쓰기 때문이다.
- 클라이언트 `isoDay`(text.js) — 받은 from/to 를 한 번 더 씻는다. 선생님이 새
  Apps Script 를 배포하기 전까지 옛 서버가 살아 있어도 화면은 바로 맞게 보인다.

오염된 값 그대로 넣고 돌려 봤다 — 면접 11/21 확정(꽉 채운 칠), 모의면접 11/18·19
(모의 칠)가 정확한 날에 선다. 하루 밀림 없음.

### 학생 카드에도 상세가 뜬다 — 읽는 자리로만

카드의 「자세히」를 누르면 연도별 추이 · 인원과 경쟁률 · 일정(원서 마감·발표 포함) ·
수능최저 원문 · 지원 자격이 뜬다. 숫자는 선생님 화면과 **같은 요약**(summaryOf →
summarize)에서 온다. 선생님 상세(card.js)를 그대로 가져오지 않은 까닭 — 결과 저장·
메모 확정 같은 교사 단추가 딸려 오는데 학생 링크로는 서버가 안 받는다. 눌리는데 안
되는 단추는 없느니만 못하다. 적는 것은 카드에 이미 다 있다.

**별칭도 학생에게 간다.** 선생님이 점검에서 이어 준 학과가 여태 학생 화면에는 안
닿아서, 같은 지원이 선생님 화면에는 작년 참고선이 붙는데 학생 화면에는 「자료
없음」이었다. `studentView_` 가 별칭을 함께 내려주고(학번 없는 자료라 새는 것이
없다), 학생 쪽 `summaryOf` 가 store.link 와 같은 규칙으로 탄다.

### 스스로 한 코드리뷰에서 나온 열 가지

전 커밋 다섯 개를 적대적으로 다시 읽혔다. 살아남은 것들 —

1. **면접·서류 가림이 「후보가 전부 어긋날 때」 조용히 꺼졌다.** `if (fits.length)`
   였다 — 서류형 하나만 남은 학과에서 면접형 지원이 그 서류형에 `sim` 으로 붙었다.
   막으려던 바로 그 오연결이다. 이제 전부 어긋나면 `none` 으로 비우고 무엇이
   있었는지 적는다.
2. **보고서가 전문대·특수대를 통째로 떨어뜨렸다.** 6칸(rank)만 세도록 바꾸면서,
   순위를 안 매기는 전문대(`tray`)가 같이 빠졌다. 전문대에 붙어 등록한 학생이
   보고서 셋에서 사라진다. 6칸 + 전문대·특수대(보관 제외)를 센다.
3. **학생 쪽 다부처 별칭 참고선이 죽은 코드였다.** 참고선을 만들어 놓고
   `confidence` 를 안 바꿔서 summarize 가 「자료 없음」 갈래에서 버렸다.
   store.link 와 글자까지 같게 맞췄다 — 이제 「묶이기 전 참고」가 학생 카드에도 뜬다.
4. **6칸 카드는 try 밖에 있었다.** 후보 목록만 감싸서, 순위에 올라간 카드가 터지면
   보드가 통째로 비는 실패가 제일 눈에 띄는 자리에 그대로 남아 있었다.
5. **「다르면 고쳐서 저장하라」면서 달력을 그 하루로 잠갔다.** 확정일에 min=max 를
   걸어서다. 기간일 때만 건다.
6. **순위 충돌 판별이 서버의 한국어 문구(/그 사이에/)에 걸려 있었다.** 문구를
   다듬는 순간 자동 재불러오기가 조용히 죽는 함정이다. 서버가 주는 `stale` 값을
   그대로 나른다. 그리고 **판단해서 거절한 응답은 재시도하지 않는다** — 같은 답이
   나올 왕복을 두 번 더 하던 것을 끊었다.
7. **첫 배치 경합이 seen 검사를 빠져나갔다.** 배치가 하나도 없던 학생을 보던
   화면은 `seen=''` 인데 `if (p.seen &&…)` 가 하필 그 경우만 검사를 건너뛰었다.
   빈 글자도 검사한다(옛 화면의 「안 보냄」은 여전히 통과).
8. **배치 쓰기 길이 둘이었다.** `place()` 가 잠금도 seen 도 없는 옛 setState 로
   따로 썼다. 이제 `placeMany` 의 낱장 꼴일 뿐이다 — 길은 하나다.
9. 있지도 않은 상태 `sched-loose` 를 확인하던 죽은 가지 정리.
10. 학생 화면 `summaryOf` 에 캐시. 교사 store 의 linkCache 와 같은 자리 —
    카드마다 서너 번씩 전체 연결을 다시 계산하고 있었다.

1·7 은 테스트로 못 박았다(`match.test.mjs` · `auth.test.mjs`).

### 「최저 있음」 대신 「최저 3합 12」

「있음」은 훑는 눈에 아무것도 말해 주지 않는다. 기준 글 465가지를 다 넣고 쟀다 —
「N개 영역 등급 합 M」·「N개 영역 각 M등급」·「N개 영역 M등급 이내」 세 꼴로
**91%가 한 가지 요약으로 떨어진다**(`minReqShort`, text.js).

줄이지 않는 것들이 요점이다. 의예과 분기처럼 값이 여러 개 나오는 글(하나만 고르면
거짓말), 「수학+탐구평균 1개 영역 합 4」 같은 묶음 특수형(「1합 4」는 오해), 못 읽는
글 34가지. 이들은 예전 그대로 「최저 있음」이고 원문은 상세와 꼬리표 title 에 있다.
**오표기가 미표기보다 나쁘다** — 컷 연결과 같은 원칙이고, 규칙은 시험이 못박는다.

교사 보드 꼬리표·학생 카드·학생 6칸이 같은 함수를 쓴다.

### 학생 6칸에도 숫자가 선다

대학·학과 이름만 있던 칸에 교사 6칸과 같은 뜻의 요약이 붙었다 — 「70%컷 2.24 ·
내 1.88」 한 줄과 꼬리표(최저 요약 · N단계 · 면접일). 여섯 칸을 나란히 보며
「어디가 빠듯한가」를 견주는 자리라 칸 안에 있어야 하고, 자세한 것은 아래 카드가
맡는다. 390px 휴대폰에서 격자 폭이 324px 라(재 봤다) 칸 최소를 150px 로 잡아
두 칸씩 선다.

### 탭에 돌아오면 보드가 스스로 다시 불러온다

학생이 밤에 순위·결과·면접일을 바꿔도 보드는 몰랐다 — 새로고침 단추를 눌러야
보였고, 안 누르면 지난 화면으로 상담을 시작한다. 담임의 아침은 대개 「어제 열어 둔
탭으로 돌아오기」라서, `visibilitychange` 가 다시 불러올 때다. 1분 안에 두 번은 안
부르고(Apps Script 하루 한도), 보기용 자료로 열었을 때는 안 부른다.

같이, **최근 사흘 안에 학생이 바꾼 배치**가 있으면 보드 맨 위에 한 줄로 알린다 —
「최근 사흘 안에 학생이 순위를 바꿨습니다 — 3105 …」. 사흘인 이유는 주말을 건너
월요일 아침에도 보여야 해서다. 시트 기록에는 이미 남지만 아무도 기록 탭을 안 연다.

### 발표일이 지나면 학생에게 결과를 묻는다

결과는 학생이 적는다(집 원칙). 발표일을 아는 것은 도구다 — 전형일정표·즐겨찾기가
1단계·최종 발표일을 준다. 발표일이 지났는데 결과 칸이 비어 있으면 학생 화면 맨 위
「결과를 적어 주세요」 판에 그 지원이 서고, 「적으러 가기」가 카드로 내려 준다.
1단계는 단계별전형에만 묻고(일괄은 1단계 발표가 없다), 최종까지 적은 지원은 빠진다.
발표 전에는 판 자체가 안 그려진다 — 켜고 끄는 단추가 없는 까닭이다(「곧 있습니다」와
같은 원리).

덤으로 `todayISO` 가 UTC 로 잘려 **자정~아침 9시에 어제 날짜**가 나오던 것을
고쳤다 — D-day 와 「곧 있습니다」가 아침에 하루씩 틀렸다.

### 학생 화면 점검 — 전교과가 본인에게 안 가고 있었다

「성적」 탭을 넣을 때 보드는 store 가 병합하는데, **학생 화면은 서버가 준 student
를 그대로 쓴다.** 그래서 같은 학생이 교사 화면에는 전교과 6.10이 서고 제 화면에는
빈칸이었다 — 두 화면이 같은 지원을 두고 다른 말을 하는 자리다. 서버의 학생
응답에서 **그 학생 것 하나만** 병합한다(이름이 다르면 안 붙이는 규칙 그대로,
캐시가 든 객체는 베껴서 고친다). auth.test 여섯 개가 지킨다.

공개 자료(입결·모집요강)도 보드처럼 서버 응답과 **병렬로** 받는다 — 직렬이면
체감 로딩이 둘의 합이다.

화면 손질 셋 (`web-ui-standard`) —

- **꼬리표가 입력칸처럼 보였다.** 좁은 폭에서 `.row` 가 한 칸 격자라 「D-10」·
  「겹칠 수 있음」 꼬리표가 **가로로 늘어나** 테두리 친 입력칸과 똑같이 생겼다.
  학생이 누르고 적으려 드는 모양이다. 꼬리표는 제 크기로 세우고
  (`justify-self: start`), 학생 화면의 줄은 글+꼬리표 둘뿐이라 좁은 폰에서도
  두 칸으로 둔다 — 다가오는 일정 패널이 663→436px 로 줄었다.
- **머리줄 한 줄.** 학생 화면은 단추가 「인쇄」 하나뿐인데 보드와 같은 규칙으로
  줄을 바꿔 세 줄이었다.
- **수능 D-n.** 머리줄에 「3201 ○○○ · 수능 D-79」. 지났으면 안 적는다.

공통 일정 표는 `board/keydates.js` **한 곳**으로 옮겼다 — 교사 달력과 학생 화면이
수능 날짜를 따로 들고 있으면 언젠가 다른 말을 한다.

### 학생 화면이 컴퓨터에서 두 단이 된다

640px 한 줄기는 휴대폰 기준인데 컴퓨터로 열면 좌우가 텅 빈 채 아래로만 길게
늘어졌다. 900px부터 카드 목록이 두 단 격자가 되고(6칸은 한 줄에 여섯), 문서
전체 길이가 1280px 기준 **9,374px → 4,568px** 로 준다. 간격은 그리드 gap 이라
구형 iOS 의 flex gap 함정과 무관하고, 좁은 화면은 예전 그대로 한 줄기 목록이다.

### 값이 정해진 입력은 접는다 (`foldRow`)

카드마다 면접일·접수번호·결과·수험번호 입력칸이 다 펼쳐져 있으니 **지금 계절의
할 일(순위 정하기)이 소음에 묻혔다.** 이제 입력 조각은 전부 `details.fold-row`
한 줄로 접힌다 — **접힌 줄(summary)이 값을 대신 말한다.**

- 값이 있으면 본문 글씨: 「면접일 · 11/27(금) · 확정」 「접수번호 · 12345」
  「결과 · 최초합격 · 예비 3번」. 고칠 때만 펴서 고친다.
- 아직 할 일이면(끝말이 「적기」) 호박색 링크 모양 — `foldRow` 가 끝말로
  `todo` 표시를 단다. 상태말(확인 대기·일정표)도 접힌 줄에 그대로 실린다.
- 순위 고르개만 펼친 채 남는다 — 지금 계절의 핵심 입력이라서.
- 메모는 주고받은 목록이 있을 때만 「메모」 머리말과 목록이 서고, 빈 카드는
  「메모 적기」 한 줄뿐이다(안내문은 편 뒤에야 나온다).
- 발표 리마인드의 「적으러 가기」는 그 카드의 결과 접힘을 **펴고** 고르개에
  초점을 준다 — 접힌 채 스크롤만 하면 어디에 적는지 안 보인다.
- **인쇄는 반대다.** `.date-add`(할 일 줄)는 종이에서 빠지지만 `.fold-row`
  접힌 줄은 값을 담고 있어 **검정 민글로 실린다.** 화면 규칙(`[open]`·`.todo`)이
  특이도로 인쇄 규칙을 이기지 않게 세 꼴을 다 나열해 눌렀다.

접은 뒤에도 접힘마다 제 줄(44px)을 차지해 카드 아래가 세로로 늘어졌다.
그렇게 한 줄로 모으니 이번에는 **밑줄 링크 서너 개가 나열된 꼴**이 됐다 —
접힌 글줄을 링크가 아니라 단추(.btn)의 기하(6px 모서리·1px 테두리)를 빌린
조용한 칩으로 바꿨다. 값이 있는 칩은 본문색, 할 일 칩은 호박색 글자.
교사 일정 탭의 빈 날짜 입력(면접일·모의면접)도 같은 칩·띠로 접는다.
**닫힌 접힘은 한 줄 띠(`fold-strip`)에 칩처럼 나란히 서고, 누른 것만 제
줄(flex-basis 100%)로 내려와 펴진다.** 간격은 flex gap 이 아니라 마진 —
구형 iOS(14.5 미만)는 flex gap 을 조용히 무시해 칩이 붙는다. 메모는 주고받은
목록이 있으면 제 블록으로 남고, 빈 카드에서만 칩으로 띠에 들어간다.

첫 카드 높이가 390px 폭에서 **892 → 408px**, 문서 전체가 7,275 → 5,535px
(1280px 두 단에서는 2,905px)로 준다.

### 전문대의 자리 — 기본은 후보, 「지원」은 6칸 아래 카드

전문대·과기원이 「수시 6회 밖」 묶음에 한 덩이로 있어서, 원서를 낸 것과 아직
고민 중인 것이 안 갈렸다. 이제 **기본은 후보(pool)** 고, 선생님이 「지원」으로
올린 것(tray)만 **6칸 바로 아래 카드로** 선다 — 교사 보드와 학생 화면 같은
자리다. 옮기기 고르개에 「후보」가 생겼고, 아래 묶음은 「전문대·특수대 후보」로
이름을 바꿨다.

tray 가 곧 「원서를 냈다」이므로 셋이 이것을 따라간다: 보고서·대장 집계
(후보 전문대는 더는 안 센다), 면접 일정 대상(후보 전문대에 면접 칸을 안
세운다), 달력의 예정 칩. 화면에 보이는 것과 세는 것이 같아졌다.

### 면접 일정은 순위에 넣은 지원만 · 학과 탐색

**면접 날짜는 순위(1~6칸)의 일이다.** 후보 카드에도 빈 「면접일 적기」와
일정표 예정이 서니 학생이 아직 넣지도 않은 원서의 면접 날짜를 고민했다.
빈 칸·일정표 예정·달력 예정 칩·「곧 있습니다」를 순위(+6회 밖) 지원으로
좁혔다 — 학생 카드(`schedTarget`)·교사 면접 준비·달력이 같은 규칙.
사람이 이미 잡은 날짜는 어디서든 보인다(값은 안 버린다). 후보를 순위로
올리면 그 자리에서 날짜 칸이 생긴다.

**학과 탐색(교사 전용, `explore.js`).** 「이 학과 쓰는 애가 우리 반에 또
있나」를 학생을 하나씩 눌러 확인하던 것을, 학과가 줄이 되고 학생이 칩이
되는 판으로 만들었다. 사람 수 기준으로 몰린 학과가 위로, 같은 학과에
전형만 달리 두 번 지원한 학생은 칩 하나(「후보 외 1」, 전형들은 title).
칩을 누르면 그 학생의 보드로 간다. 대학·학과 글자 찾기. 보관은 뺀다.
학생 화면에는 두지 않는다 — 다른 학생이 보이면 안 된다(CONTRACT).

이어서 다듬은 것 셋.

- **돌아오는 길** — 칩으로 학생 보드에 다녀오면 화면 단추 줄에 「← 탐색으로」가
  생긴다(건너뛴 뒤에만). 찾기 글과 스크롤 자리가 그대로 복원된다. 화면을
  먼저 바꾸고 select 를 불러야 한다 — 반대로 하면 탐색이 다시 그려지며
  방금 켠 단추를 도로 숨긴다.
- **학과 줄에 잰 값 요약** — 「3명 · 전교과 2.11~3.42 · 작년 70%컷 2.26~2.54」.
  내신은 전교과(전형 무관 공통 잣대)가 다 있으면 전교과, 아니면 환산등급으로
  적고 **어느 잣대인지 같이 적는다**. 컷은 전형별로 다르면 범위, 내역은
  title 에. 유의검정은 안 한다(§6) — 요약값뿐이라 p값은 지어내기다. 범위와
  중앙만 잰 값 그대로 보이고 판단은 상담이 한다. 학과 이름을 누르면 대표
  지원의 상세(연도별 추이·충원·전형 방법)가 뜬다 — 추이를 여기 또 그리지 않는다.
- **가상 분포 그림(접어 둠)** — 검토판 맨 아래에 전형별 곡선 한 장. 두 컷 사이만
  실선이고 그 밖은 점선이며, 해마다의 곡선을 옅게 겹친다. 우리 학생이 어디 서는지가
  번호로 찍힌다. 그릴 만한지 재고 넣었다 — §6 「학과 탐색의 가상 분포 그림」.
- **순위 변경 공지를 닫을 수 있다** — 「최근 사흘…」 배너에 닫기가 생겼다.
  닫기는 「여기까지 봤다」다: 마지막 변경 시각을 이 컴퓨터에 기억하고
  (`board.movesSeen`), 그보다 늦은 변경이 오면 다시 선다. 이름을 누르면
  그 학생 보드로 간다.

### 전형 방법을 명시한다 (`methodLine` · `interviewShare`)

상담 양식들(박석경 「교직 실전 매뉴얼」 등)이 잘하는 것 하나 — **이 전형이
무엇으로 뽑는지**를 칸으로 박아 둔다. 우리 화면은 「2단계」라는 단계 수만 있었다.
모집요강에 이미 다 있다: 전형방법1 은 16,873줄 전부(「서류100」꼴), 선발비율1 은
단계전형의 배수×100, 전형방법2 는 2단계 줄, 학생부 구성(교과 90 + 출결 10)과
반영 교과 원문(공통일반·진로선택)도 58%.

- **꼬리표** — 「2단계」가 「2단계 면접50%」로. 면접이 당락에 몇 % 걸리는지
  훑는 눈에 바로 보인다. 전형 방법 전문은 제목(title)에 있다.
- **상세** — 「전형 방법: 1단계 서류100 (3배수) → 2단계 서류50+면접50」 줄과
  「학생부 구성」, 「반영 교과」 원문 블록(교과·학생 두 화면 같은 꼴). 방법1에
  「1단계」「(3배수)」가 이미 적힌 줄은 떼서 겹말을 막고, 방법2가 문장 전체인
  줄(73건)은 그대로 싣는다. 못 읽으면 null — 오표기가 미표기보다 나쁘다.
- **일괄+면접 사각지대** — 「학생부60+면접40」처럼 일괄인데 면접이 든 전형이
  실제 지원 500건에 **18건**(항공서비스·간호 계열). 전형단계 ≥ 2 규칙으로는
  면접 표시도 날짜 칸도 안 섰다. 이제 전형 방법 글의 면접(`methodHasInterview`)도
  근거가 되어 세 화면(학생 카드·교사 꼬리표·일정 면접 준비)이 같이 세운다.
- 「면접100」이 전체 자료에 12건 있어 백분율을 두 자리로 끊으면 10으로 오독한다 —
  세 자리 + ≤100 검사로 잡았다.

실제 441건(모집요강 붙는 지원 전부)에서 methodLine 이 null 인 줄은 0, 조립된
줄 꼴 38가지를 눈으로 확인했다. 지원 의도·소신/적정/안전 칸은 넣지 않았다 —
학생 입력을 접던 흐름과 반대라, 필요해지면 메모가 그 자리다.

### V9.7 상담카드에서 배워 온 세 가지

다른 선생님들이 만든 「2027 수시 지원 상담카드 V9.7」(운정고 이재규 외)을 놓고
우리 것과 견줬다. **자료는 안 가져왔다** — 그쪽의 대학어디가 입결(남악고 김현석
정리)·전형 자료는 남의 정리물이라 출처 문제가 있고, 재 보니 우리 모집요강에
같은 값이 이미 있었다. 가져온 것은 세 가지 생각이다.

**하나 — 충원(추가합격) 추이** (`fillTrend`). 그쪽 「과거 입결·충원 추이」 부록의
핵심. 우리 모집요강의 추합26·25·24 로 그대로 그려진다(추합 76·65·62%, 실질
계산 통과 74·63·60%). 상세에 연도 | 모집 | 경쟁률 | 추가 합격 | 충원율 | 실질
표가 선다. 충원율 = 추합 ÷ 모집 × 100. 추합란이 누적 예비번호로 보이는 해는
실질경쟁률과 같은 가드로 칸을 비운다. 12월 예비번호 상담에서 「이 전형은
예비가 모집의 94%까지 돌았다」를 바로 읽는 자리다.

**둘 — 전문대 수시의 규칙.** 접수 기간이 전 대학 공통(1차 9. 7~9. 30 ·
2차 11. 11~11. 25, 한국전문대학교육협의회)이고, 지원 횟수 제한이 없으며,
**수시에 붙어 등록하면 정시·추가모집 지원이 막힌다** — 몰랐다가는 정시를
통째로 잃는 규칙인데 화면 어디에도 없었다. 전문대 지원이 있는 학생 화면에
안내판이, 교사 상세에 「전문대 공통 일정」 줄이 선다(자료 연결과 무관하게).

**셋 — 수시 6회 제한 밖 대학** (`outsideLimit`). 전문대·특수대는 학교유형으로
걸렀지만, 과학기술원(KAIST·GIST·DGIST·UNIST)과 한국에너지공대는 **학교유형이
「일반대」로 들어와 6칸을 먹게 되어 있었다.** 이름으로 거르고, 6회 밖 판정을
쓰던 여섯 군데(학생 순위·끌기, 교사 나눔·이동, 내보내기 둘)를 한 함수로 모았다.
실제 자료에는 아직 0건 — 예방 규칙이라 꼬리표(「수시 6회에 안 셈」)도 단다.

안 가져온 것 — 대학별 환산등급 칸(자동 계산이 안 되는 값을 손으로 적는 칸은
우리 원칙과 어긋난다), 학생 작성 → 편집파일 인계 흐름(우리는 시트가 그 자리),
상담카드 A4 한 장 인쇄(내보내기·학생 인쇄가 이미 있어 겹치면 따로 판단).

### 학생이 적은 것이 학생에게 안 돌아오던 것 — 학번이 빈 채 저장됐다

학생이 면접일을 넣고 담임이 「맞습니다」까지 눌렀는데 학생 화면은 「아직 날짜가
없습니다」였다. 전 과정을 그대로 밟아 재현했다 — `studentDate` 가 요청을 그대로
서버 함수에 넘겨서 **일정 행의 학번 칸이 비었다.** 담임 보드는 전체 행을 읽으니
보였고 확정도 됐지만, 학생 화면은 학번으로 제 것만 걸러 받아서 학생에게만 안
돌아왔다.

같은 꼴 구멍이 둘 더 있었다. `studentField`(수험번호·최종경쟁률)는 학번이 없어
**저장 자체가 「학번이 필요합니다」로 실패**했고, 생년월일은 id 가 빈 값이라
지원 소유 검사에서 먼저 끊겨 **저장할 길이 아예 없었다.**

- 학번은 **서버가 토큰에서 채운다.** 클라이언트가 보낸 학번은 버린다 — 남의
  학번을 적어 보낼 수 있는 값이다.
- 생년월일만 지원 소유 검사에서 뺀다(학생 한 명에 하나라 id 가 없다).
- 이미 학번이 빈 채 저장된 일정 행은 **읽을 때 거둔다** — id 가 내 지원이면
  내 것이 맞다(안정키가 학번을 씨앗에 품는다). 학생이 다시 적을 필요가 없다.

세 경우 모두 auth.test 가 전 과정(넣기 → 확정 → 되돌아오기)으로 못박는다.
**이 수정은 Apps Script 재배포가 필요하다.**

재배포가 됐는지 확인할 길이 여태 없어서 `ping` 이 `ver`(코드 판 날짜)를
돌려주게 했다 — 배포 뒤 브라우저에서 `<웹앱 주소>?action=ping&key=<열쇠>` 를
열면 새 판인지 바로 보인다. Code.gs 를 고칠 때마다 `CODE_VER` 를 올린다.

### 일정 탭 면접 정리

- 달력 칩과 「면접 준비」 머리에 **학과가 빠져 있었다** — 한 대학 두 학과를
  지원한 학생이면 어느 면접인지 못 가른다. 칩은 학과를 둘째 줄로 달고(한 줄로
  붙이면 칸 폭에서 말줄임표에 먹힌다), 반 전체 「날짜순」에도 학과를 넣었다.
- **달력 칩을 누르면 그 지원의 카드 상세가 뜬다.** 상세 덮개는 탭과 무관하게
  뜨는 자리라 일정 탭 위에서 바로 열리고, 거기 「면접·실기 날짜 넣기 · 고치기」가
  이미 있다. Esc 로 닫힌다.

### 달력의 공통 일정과 크게 보기

지원마다 다른 면접·논술과 달리 **모두에게 같은 날**이 있다 — 원서접수(9/7~11)·
수능 예비소집(11/18)·**수능(11/19)**·성적 통지(12/11)·발표 마감(12/18)·
등록(12/21~23)·미등록 충원(12/24~29)·충원 마감(12/30). 출처는 교육부 수능 시행일
발표와 대교협 2027 대입전형 기본사항이고, 발표 마감 12/18 은 전형일정표 자료의
실측(마감일 발표 302개교)과도 맞는다. `schedule.js` 의 `KEY_DATES` 한 곳에 있고
**해가 바뀌면 이 표부터 간다.**

그리는 규칙 — 지원의 칩(누르면 카드)과 달리 누를 카드가 없으니 단추가 아니고,
칸의 **윗줄**로 깔린다. 기간은 윗줄이 날마다 이어지고 이름은 첫날에만 적는다
(닷새 내리 같은 글자는 소음이다). 수능만 바탕까지 칠한다 — 이 달력에서 하루를
통째로 갖는 날은 그날뿐이다. 짧은 이름은 칸에, 온이름은 제목(title)에. 달력
머리에 수능 D-n 을 적되 지나갔으면 안 적는다. 공통 일정의 달(9·11·12월)은 지원
일정이 없어도 서고, 잡힌 날짜가 하나도 없는 반 화면에도 이 달력만은 그린다.

**달은 하나씩, 잘라내기 없이.** 처음에는 두 달을 나란히 놓고(268px 다단) 칩을
말줄임으로 잘랐다 — 칸이 44px 까지 좁아져 「숭실…」「320…」만 남았다. 안 보이는
글씨는 없는 글씨다. 다단을 버리고 달이 화면 폭을 다 쓰게 하니, 잠깐 만들었던
「크게 보기」 단추도 할 일이 없어 걷었다. 판은 하나고 그 하나에서 글이 다 보인다.
칩은 자르지 않고 접는다.

**좁은 폰에서는 달이 제 칸 안에서 옆으로 구른다**(§7 — 표는 제 안에서 굴러야
한다). 390px 에 일곱 칸을 우겨 넣으면 칸이 40px 이 되어 한글이 한 자씩 세로로
깨진다 — 잘라내기의 반대쪽 구덩이다. `.cal` 에 최소 폭(616px)을 못박고 넘치면
달 상자가 구른다. 본문은 안 구른다(`.months` 격자 칸에 `minmax(0,1fr)` — §7 의
그 함정이 여기서도 나왔다).

훑는 눈을 돕는 것 셋 — **오늘**은 숫자에 앰버 고리 하나(색을 더 쓰지 않고 오늘만
세운다), **주말**은 바탕을 반 단계 낮추고, **지나간 날**은 숫자만 옅어진다(칩은
그대로 — 지나간 면접도 기록이다). 마지막 날 뒤의 빈 자리도 채운다 — 안 채우면
격자 바탕이 드러나 마지막 줄만 짙은 덩어리로 끝난다.

### 반 달력 — 모의면접이 몰린 날이 보인다

겹침 판정은 학생 한 명 안에서만 본다. 그런데 11월의 실제 문제는 **여러 학생의
모의면접이 같은 날 몰리는 것**이다 — 교실도 담임의 몸도 하나다. 반 전체 일정
(학생 미선택)에 「날짜순」 아래로 달력을 얹었다. 칩 둘째 줄이 학생 화면·개인
달력에서는 학과, 반 달력에서는 **학번·이름**이다 — 그 자리에서 갈라 주는 것이
다르기 때문이다. 칩을 누르면 그 지원의 카드가 열리는 것은 같다.

**모의면접을 카드 상세에서 바로 잡는다.** 「면접·실기 날짜 넣기 · 고치기」의 종류에
모의면접이 붙었다 — 몇 차인지는 저장할 때 빈 차수를 찾아 정한다(모의면접1~5,
일정 탭과 같은 칸 이름). 다 찼으면 조용히 덮지 않고 말한다.

**일정 탭이 그대로 A4 로 나온다.** 배경 칠이 종이에 안 찍히므로 인쇄에서 칩은
테두리로 성립하고, 달력 한 달과 대학 덩이는 페이지를 넘지 않는다. 담임 책상용
주간표는 브라우저 인쇄면 된다.

### 내보내기는 **6칸에 넣은 것만** 센다

즐겨찾기에는 한 학생이 스무 곳 넘게 담아 둔다. 그걸 다 세면 「우리 학교가
서울시립대에 8명 넣었다」 같은 숫자가 나오는데, 실제로 원서를 낸 것은 그중 한둘이다.
진학 대장은 처음부터 6칸만 실었는데(`ordered`) **보고서 셋은 전부를 세고 있었다.**
`rowsForReport()` 도 `ordered()` 를 지나가게 했다 — 실제 자료에서 502건이 219건이 된다.
후보와 보관은 상담하며 견주던 목록이지 지원이 아니다.

### 학생이 적고 담임이 확인한다 — 그런데 메모만 다르다

학생 화면에서 넣는 것은 넷이다.

| | 넣으면 | 담임이 |
|---|---|---|
| 면접·실기 확정일 | `pending` | 「맞습니다」로 확정 |
| 수험번호·최종경쟁률·생년월일 | `student` | 「맞습니다」로 확정 |
| 결과 | `student` | 「맞습니다」로 확정 |
| **메모** | 바로 남는다 | **누를 것이 없다** |

앞의 셋은 값이라 틀리면 판단이 흔들린다 — 잘못 적은 날짜 하나로 겹침 판정이
뒤집힌다. 그래서 확인을 거친다. 메모는 값이 아니라 할 말이라 확인할 것이 없다.

그렇다고 그냥 두면 **학생이 적은 것을 선생님이 카드를 열기 전까지 모른다.**
「안 읽음」 칸을 시트에 더하는 대신 **대화로 읽는다** — 그 지원의 메모를 시간순으로
놓고 마지막 말이 학생 것이면 아직 답이 안 간 것이다(`store.unansweredNotes`).
선생님이 메모를 하나 달면 저절로 목록에서 빠진다. 새 칸이 없어 시트가 그대로고,
쌓이기만 하는 목록이 되지도 않는다.

메모는 선생님 것과 **같은 탭에 같은 모양으로** 쌓이고 카드마다 한 줄로 섞여 보인다.
누가 적었는지는 `by`(「3201 학생」)가 지고, 두 화면이 그걸로 갈라 적는다.
지우는 것은 자기가 적은 것만 — 서버가 학번과 `by` 를 둘 다 본다. 학번만 보면
선생님이 그 학생에게 적어 둔 메모까지 학생이 지울 수 있다.

**진학 대장 「특이사항」에는 학생 메모를 안 싣는다.** 결재로 올라가는 문서다.
접수번호도 뺀다 — 저장하는 자리가 메모 탭이라 같이 쌓이지만 그건 칸이다.

**순수 함수를 갈라 둔 까닭** — `match` · `stats` · `confidence` 는 화면을 모른다.
그래서 node 에서 실제 자료로 돌려 볼 수 있고, `*.test.mjs` 가 규칙을 지킨다.
브라우저를 띄우지 않고도 「이 규칙이 아직 맞나」를 물을 수 있어야 한다.

### 읽는 속도 — 시간이 어디서 가는지 재고 셋을 고쳤다

보드가 뜨기까지의 시간을 쪼개 보면, 공개 자료는 주범이 아니었다 — Pages 가 gzip
으로 줘서 입결 9.3MB 의 실전송은 1.27MB, 넷을 다 합쳐도 2.1MB 고 브라우저가
10분 캐시한다. 주범은 **Apps Script 요청 하나**다: 콜드스타트 1~3초에, 요청 안에서
시트 읽기가 12~14회(회당 100~300ms, 전부 직렬), 외부 원본이면 openById 까지.

세 가지를 고쳤다.

1. **공개 자료를 서버 응답과 병렬로** (`store.load`). 예전에는 서버 응답을 다
   기다린 뒤에야 입결을 받기 시작해 체감 로딩이 둘의 합이었다. 서로 독립이라
   같이 시작하면 긴 쪽 하나가 된다.
2. **설정 탭을 요청에 한 번만** (`configTab_`). 열쇠(C2)·계정 명단(A열)·원본
   주소(B2)를 서너 번 따로 읽고 있었다. 메모는 요청 단위다 — `handle_` 들머리에서
   비워 어떤 실행 환경에서도 묵은 설정이 안 샌다.
3. **원본 파싱을 5분 캐시** (`sourceParsed_` + CacheService). 제일 비싼 두 가지
   (원본 열기 + 큰 표 파싱)의 내용은 엑셀을 갈아끼울 때만 바뀐다. **살아 움직이는
   탭(배치·메모·결과·일정·입력·별칭·설정)은 캐시하지 않는다** — 방금 쓴 것이 다음
   요청에 바로 보여야 한다. 학생 링크와 쓰기 검증(`ownsApp_`)도 같은 캐시를 본다.

캐시의 규칙 셋. ① 키당 100KB 한계라 gzip 해 조각으로 담고, 쓰는 중의 읽기가
반쪽을 못 집게 조각 이름에 도장을 찍는다 — 조각이 하나라도 밀려났으면 통째로
없는 것으로 친다. ② 보드의 「새로고침」 단추만 fresh=1 로 캐시를 건너뛴다 —
엑셀을 갈아끼운 직후용이다. 자동 새로고침(탭 복귀)은 캐시를 쓴다. ③ **파싱
결과의 모양이 바뀌면 `SRC_CACHE_KEY` 의 판을 올린다** — 안 올리면 새 코드가
옛 모양의 캐시를 5분 내보낸다. 왕복·조각 유실·fresh 는 auth.test 가 지킨다.

### 왜 JSONP 인가

Apps Script 웹앱은 CORS 헤더를 제대로 주지 않는다. `fetch` 로는 못 부른다.
그래서 `<script>` 태그로 부른다. 오래된 방식이지만 여기서는 이것뿐이다.

### 안정키

지원 한 건의 id 는 이렇게 만든다.

```
id = sha1( 학번 + "" + 대학 + "" + 세부전형 + "" + 모집단위 ).slice(0,16)
```

엑셀 원본을 새로 받아 갈아끼워도 **순위·메모·결과가 그대로 붙어 있어야** 하기 때문이다.
행 번호로 잡으면 원본을 갈아끼우는 순간 전부 어긋난다.

구분자가 공백이 아닌 까닭 — 공백이면 `가 나`+`다` 와 `가`+`나 다` 가 같은 씨앗이 되어
서로 다른 지원의 메모가 뒤섞인다.

---

## 5. 이름 맞추기 — 이 저장소에서 제일 어려운 부분

세 자료가 같은 대학·학과를 서로 다르게 부른다.

```
즐겨찾기   한국외국어대학교(용인) - 글로벌캠퍼스   통합모집(자유전공)
입결       한국외대(글)                        자유전공학부
모집요강   한국외대                            자유전공
```

`board/match.js` 가 이걸 맞춘다. 규칙은 `scripts/build_xref.py` 의 `norm_dept` 와
**같아야 하고**, `board/match.test.mjs` 가 그걸 지킨다. 한쪽을 고치면 다른 쪽도 고친다.

### 원칙 — 오연결이 미연결보다 나쁘다

애매하면 잇지 않고 「연결 안 됨」으로 남긴다. 이 원칙 때문에 되돌린 것이 여럿이다.

- 전형일정표에서 유형(교과/종합)만 같아도 고사일을 붙였더니, **면접이 없는 서류형
  전형에 그 대학 종합전형 면접일이 붙었다.** 있지도 않은 겹침 경고가 떴다.
  → 전형 이름이 맞을 때만 쓰고, 못 맞춘 값은 「이 전형의 날짜가 아닙니다」라고 적는다.
- PDF 표에서 한 칸이 여러 줄에 걸치면 위아래 어느 행 것인지 가릴 수 없다.
  → 행 자기 칸의 날짜만 쓰고 나머지는 원문 그대로 보여 준다.
- 못 붙인 학과를 닮은 이름으로 자동으로 이었더니 「영어영문학과」와 「영어교육과」가
  붙었다. → **점검 화면**에서 후보를 점수와 함께 늘어놓고 사람이 고른다.
- 학과만 맞추고 전형은 아무 줄이나 썼더니 교과 지원자 카드에 종합의 컷이 떴다.
  학과의 73% 가 입결에 전형이 여럿이라 늘 그럴듯하게 틀렸다.
  → 지원한 전형의 줄만 쓰고, 못 가리면 **비운다** (바로 아래).

### 전형까지 맞춰야 한다 — 학과만 맞추면 옆 전형의 컷이 붙는다

입결은 **(대학·학과)로만** 묶여 있다. 한 학과 안에 전형이 여럿 들어 있고, 학과키
9,825곳 가운데 **73.0%** 가 가장 최근 해에만도 전형이 둘 이상이다. 그중 **23.6%** 는
전형끼리 70%컷이 1등급 넘게 벌어진다.

```
전남대 영어영문  2026   교과(일반) 3.20  ·  종합(고교생활Ⅰ) 3.58
가천대 경영학과  2026   교과(학생부) 2.52 ·  종합(바람개비) 3.90
```

그래서 「연도순으로 정렬한 뒤 마지막 줄」을 쓰면 교과로 넣은 학생 카드에 종합의 컷이
뜬다. 그럴듯한 숫자라 아무도 못 알아챈다. `match.js` 의 `pickIpgyeol()` 이 지원한
전형의 줄만 골라 `summary.mine` 에 담고, 컷·경쟁률·모집인원은 **거기서만** 온다.
묶는 일은 `typeGroups()` 가 한다 — 이름 정규화와 「해가 바뀌며 이름이 바뀐 것 잇기」가
거기 다 들어 있고, 연도별 추이 표도 같은 함수를 본다.

고르는 차례와 화면에 적는 말

| `typeFit` | 어떻게 골랐나 | 화면 |
|---|---|---|
| `exact` | 정규화한 이름이 같다 | `입결 · 교과(일반)` |
| `near` | 한쪽이 다른 쪽을 감싸고, 겹치는 글자가 가장 긴 묶음이 하나뿐 | `… 이름이 조금 다르지만 같은 전형으로 봤습니다` |
| `cat` | 이름은 못 맞췄고 유형(교과/종합/논술/실기)이 같은 묶음이 하나뿐 | `… 전형 이름은 못 맞추고 유형만 같습니다` + 카드에 표시 |
| `only` | 이 학과 입결에 전형이 애초에 하나뿐 | `… 전형이 이것 하나뿐입니다` + 카드에 표시 |
| `none` | 위 어느 것도 아니다 | **컷·경쟁률을 비운다.** 「전형 못 가림」 꼬리표 + 후보 이름들 |

겹치는 글자 **길이**로 재는 까닭 — `교과(일반)` 은 `일반` 도 감싼다. 길이를 안 보면
`일반` 과 `교과일반` 이 비겨서 못 고른다.

모집요강 줄로 재 보면 exact 62.8% · near 20.9% · cat 3.2% · only 0.5% · **none 12.8%**.
예전 방식과 다른 줄을 고른 것이 68.9%, 그중 컷이 1등급 이상 달라진 것이 12.2% 다.

`none` 일 때 빈칸으로 두는 것이 핵심이다. 옆 전형의 숫자를 앉히면 미연결이 아니라
**오연결**이 된다. 대신 「연도별 추이」 표는 전형을 다 보여 주고, 카드가 후보 이름을
짚어 준다 — 사람이 표에서 직접 읽으면 된다.

**원본이 이미 한 번 통일해 준다.** `build_ipgyeol.py` 가 대학어디가 발표자료를 정본으로
삼아 연도별 전형명 표기를 맞추고 `track_alias.json` 에 별칭 484건을 남긴다. 그 덕에
전년 대비 견줄 수 있는 행이 45.8% → 60.5%, 한 건도 못 견주던 대학이 25곳 → 14곳이 됐다.
아래 `normType`·`typeGroups` 는 그러고도 남는 것을 화면에서 잇는다 — 두 겹이다.

**원본에 카테고리만 다른 쌍둥이 행이 36쌍 있다.** 값이 전부 같은데 하나는 교과, 하나는
종합이다(꽃동네대 간호 2026 「종합(가톨릭지도자)」). 숫자가 같아 화면 값은 어느 쪽이든
같지만, `pickIpgyeol` 의 카테고리 경로가 그 묶음을 양쪽으로 인정해 교과 지원자에게
종합 전형이 붙는다. `indexIpgyeol` 이 색인할 때 전형 이름을 믿고 한 줄만 남긴다.
공백만 다른 완전중복 258행도 같이 줄인다 — 다만 **학과 이름이 다르면 안 지운다.**
`normDept` 가 한 키로 합쳐 놓은 서로 다른 학과(전남대 `국제학부(일본학전공)` ·
`국제학부(중국학전공)`)까지 지우면 없는 학과가 된다.

**전형명 통일이 뭉갠 줄 100개를 되돌렸다.** 별칭표 키가 `대학|카테고리|전형` 이라
학과가 없어서 한 대학에 일괄로 적용되는데, 두 이름이 *같은 학과 같은 해*에 다 있는
곳에서는 개명한 키가 서로 부딪힌다. 그때 한 줄이 사라지고 남은 줄은 제 이름이 아닌
개명후 이름을 달았다.

```
경성대 소프트웨어학과 2024
    종합(지역Ⅱ)   모집 4  70컷 2.67   ← 이 값이
    종합(학교생활)  모집 8  70컷 5.79   ← 이 이름을 달고 남았다. 8명짜리 줄은 사라졌다
```

100 묶음이 이랬고, 70%컷이 1등급 넘게 벌어지는 것이 16건 · 2등급 넘는 것이 5건이다.
**27건은 어려운 쪽이 사라져 카드가 실제보다 쉬워 보였다.**

`scripts/fix_track_conflict.py` 가 `review_track_conflict.csv` 를 보고 되돌린다 —
남은 줄의 이름을 제 이름으로 돌리고, 사라진 줄을 되살린다. 75,494 → 75,592행.
원인은 `build_ipgyeol.py` 쪽에도 고쳐 두었다(부딪히면 개명을 포기한다). 다만 원본
엑셀이 없어 그쪽은 아직 못 돌려 봤다 — **엑셀을 다시 돌리면 이 되돌리기도 다시
돌려야 한다.**

### 경쟁률이 1 미만인 줄 (`review_comp_anomaly.csv`) — 확인 끝, 손대지 않는다

487행이 경쟁률 1 미만이다. 엑셀이 「13:1」을 시간으로 읽어 0.542 로 뭉갠 것 아니냐는
의심이 있었지만, **재 보니 진짜 미달이다.**

모집인원 × 경쟁률을 반올림해 지원자 수를 되돌린 뒤 다시 나눠 자릿수까지 맞는지 본다.

| 무리 | 되돌린 값이 원래 값과 맞는 비율 |
|---|---|
| 경쟁률 1 미만 (487행) | **82.1%** |
| 경쟁률 1 초과 (대조군) | 78.3% |
| 같은 모집인원에 무작위 값 | 16.9% |

시간 서식으로 뭉개진 값이라면 모집인원과 이런 정합이 나올 수 없다. 대조군과 같은
수준이므로 **원래 값 그대로 쓴다.** 「시간서식의심」·「복원하면」 칸이 487행 모두
비어 있는 것도 같은 이야기다.

대신 카드가 이 사실을 **말로** 적는다 — 「0.54:1」은 훑으면 그냥 낮은 숫자로 읽히지만
상담에서는 「뽑으려던 인원보다 지원자가 적었다」는 뜻이고 컷도 그만큼 못 믿을 값이
된다. 대학·학과·전형 묶음 28,569개 가운데 미달 해가 있는 것은 432개(1.5%)이고,
그중 300개는 가장 최근 해가 미달이다. 드물어서 이 한 줄이 소음이 되지 않는다.

### 전형 이름 정규화 (`normType`)

입결의 전형 이름이 해마다 조금씩 달라진다. 전형이 둘 이상인 (대학·학과) 7,550곳
가운데 **25.8%** 가 이 꼴이다.

```
교과(일반)       ↔  교과(일반전형)
종합(고교생활Ⅰ)  ↔  종합(고교Ⅰ)
교과(지역)       ↔  교과(지역인재)
종합(고교Ⅱ)      ↔  종합(고교2)
학생부교과(일반전형) ↔ 교과(일반)      ← 즐겨찾기와 입결의 표기 차이
```

그대로 두면 연도별 추이가 한 해씩 토막 나고 「해마다 얼마나 움직였나」도 못 잰다.
**적게 지운다** — 괄호·공백·가운뎃점을 털고, 붙었다 떨어졌다 하는 꼬리
(`학생부`·`전형`·`선발`·`우수자`·`생활`·`인재`·`학생`·`모집`)만 턴다. 로마 숫자는
아라비아로 바꾼다(Ⅱ↔2). 교과와 종합, Ⅰ과 Ⅱ, 지역과 학교장추천은 서로 다른 전형이라
절대 합치지 않는다.

`학생부` 를 `학생` 보다 **먼저** 턴다. 뒤에 두면 `학생부교과` 가 `부교과` 로 남아
즐겨찾기의 `학생부교과(일반전형)` 와 입결의 `교과(일반)` 이 안 붙는다.

재 봤다 — 이름 변형 4,039건이 합쳐지고, 같은 해·같은 묶음에 카테고리(교과/종합)가
섞인 경우 **0건**, 70%컷이 2등급 넘게 벌어진 경우 **0건**. 잘못 합친 것은 없다.

#### 붙임표도 턴다

가운뎃점 네 벌을 안 털어 `normDept` 가 학과를 둘로 갈랐던 것과 같은 구멍이
`normType` 에도 있었다. 괄호·공백·가운뎃점은 털면서 **붙임표를 안 털었다.**

```
즐겨찾기  학생부종합(잠재능력우수자면접전형)  →  종합잠재능력면접
입결      종합(잠재능력-면접)                →  종합잠재능력-면접   ← 안 맞는다
```

못 맞추면 `near` 가 대신 「감싸는 이름」을 찾는데, 하필 2022~2023년의
`종합(잠재능력)` 이 걸린다. **2024년에 면접·서류로 쪼개지기 전** 전형이다.
가톨릭대(성심) 영어영문학부 카드가 2026년 컷 4.20 대신 3년 묵은 줄을 달고 있었다.

털고 재 봤다. 남아 있던 구두점은 붙임표 129가지 · `+` 6 · `'` `⋅` `_` 각 1 이다.
다 털어도 **같은 해 안에서 서로 다른 전형이 한 이름이 되는 일은 새로 안 생긴다
(72건 → 72건).** 남은 72건은 붙임표와 무관한, `인재` 를 군더더기로 털면서 생긴 옛
부딪힘이다 — 한 해에 `교과(지역인재)` 와 `교과(지역)` 이 같이 있는 선문대·가톨릭관동대 꼴.

2027 모집요강의 전형·모집단위 16,714가지를 다 돌려 본 결과다.

| | 전 | 후 |
|---|---|---|
| `exact` | 9,142 (54.7%) | **9,282 (55.5%)** |
| `near` | 2,707 (16.2%) | 2,691 (16.1%) |
| `cat` | 339 (2.0%) | 321 (1.9%) |
| `none` (못 가림) | 1,599 (9.6%) | **1,493 (8.9%)** |
| 70%컷이 카드에 뜨는 것 | 11,152 (66.7%) | **11,372 (68.0%)** |

**컷을 잃은 카드는 0장**이고 220장이 새로 얻었다. 고른 전형이 바뀐 309장은 전부
쪼개진 갈래를 제대로 찾아간 것이다.

```
단국대(죽전) 국제경영  DKU인재(면접형)   종합(DKU인재) 2023 5.25  →  DKU인재-면접 2026 3.11
연세대(미래) 의예과    강원인재(한마음)   종합(강원인재) 2026 3.66  →  강원-한마음  2024 2.62
경북대 치의예과       지역인재 학교장추천  종합(지역인재) 2026 2.07  →  지역-학교장  2025 1.40
```

연세대(미래) 것이 특히 그렇다. `강원인재` 는 25명 뽑고 경쟁률 11.4:1 인데
`강원-한마음` 은 3명에 1:1 이다. 아예 다른 전형이었다.

### 고른 전형이 학과 자료보다 일찍 끊겼으면 (`staleOf`)

카드는 고른 전형의 가장 최근 줄을 「작년 경쟁률」·「작년 70% 컷」이라 적어 왔다.
그런데 그 줄이 정말 작년 것이 아닐 때가 있다. 입결이 붙는 카드 12,294장 가운데
**895장(7.3%)** 이 그렇다.

| 까닭 | 장수 |
|---|---|
| 전형 자체가 일찍 끊겼다 | 352 |
| 전형은 이어지는데 그해 컷 칸만 비어 앞 해를 썼다 | 543 |

끊기는 까닭은 대개 **쪼개져서**다. 352장 중 170장이 그 꼴이다.

```
가톨릭대(성심)  종합(잠재능력) ~2023       →  2024부터 잠재능력-면접 · 잠재능력-서류
강원대(도계)    종합(미래인재) ~2023 5.53  →  2025까지 미래인재1 4.84 · 미래인재2 4.86
```

**이름으로 가려지면 가린다. 안 가려지면 안 잇는다.**

「미래인재면접」이 미래인재1인지 2인지는 못 가린다. 골라 주면 그럴듯한 숫자가 뜨고
아무도 못 알아챈다. 둘을 적어 두고 선생님이 「연도별 추이」에서 직접 견주게 한다.
**오연결이 미연결보다 나쁘다.**

#### 면접이냐 서류냐는 가릴 수 있다 (`pickHeir`)

갈래가 「면접형·서류형」 둘이면 이야기가 다르다. 면접형은 1단계에서 걸러 놓고
2단계에서 면접을 보니 **단계별전형**이고, 서류형은 서류만 보고 한 번에 뽑으니
**일괄전형**이다. 모집요강의 `전형단계` 가 그것을 말해 준다.

2027 모집요강의 학생부종합 가운데 이름에 면접·서류가 든 1,898줄로 쟀다.

| | 2단계 | 1단계 |
|---|---|---|
| **면접**형 | 843 | 17 |
| **서류**형 | 2 | 1,036 |

「2단계면 면접·1단계면 서류」로 읽으면 **99.0%(1,879/1,898)** 를 맞힌다.

지원한 전형의 **이름**도 같이 본다. 실제로 갈릴 일이 있는 41장에서 이름과 단계가
**41장 모두 일치했고 어긋난 것이 0장**이다. 어긋나면 안 고른다. 이름으로 가렸을
때는 같은 표시를 단 모집요강 줄의 단계로 뒷받침하고, 모집요강이 반대로 말하면
(면접형인데 일괄전형 — 위 표의 17줄 같은 것) 손 뗀다.

**전형단계 하나만으로 가리는 것은 모집요강이 한 목소리일 때뿐이다.** 이름이
두루뭉술하면(`종합(잠재능력)`) `pickMojip` 이 면접전형(2단계)과 서류전형(1단계)에
둘 다 걸려서, 맨 앞 줄의 단계를 집으면 동전 던지기가 된다.

**`among`(전형 못 가림) 후보에는 이 규칙을 안 쓴다.** 거기 후보들은 그저 「이 학과에
있는 전형」이라 끝 글자만 같은 남남이 섞인다 — 강원대(강릉) 사학과의
`지역인재서류전형` 을 `미래인재서류` 에 이어 버린다. 아예 다른 전형이다. 쪼개진
갈래는 다르다. 고른 전형의 이름을 앞에 그대로 두고 갈라져 나온 것이라 남는 물음이
「면접이냐 서류냐」 하나뿐이다.

가려낸 41장의 결과다. 컷이 움직인 폭은 **중앙 0.47 · 최대 2.60**, 1등급 넘게
움직인 것이 7장이다.

```
광운대 국제학부   광운참빛Ⅰ-면접형   종합(광운참빛) 2023 3.73  →  광운참빛-면접 2026 5.52
광운대 법학부     광운참빛Ⅰ-면접형   종합(광운참빛) 2023 3.08  →  광운참빛-면접 2026 4.50
광운대 국어국문   광운참빛Ⅱ-서류형   종합(광운참빛) 2023 3.84  →  광운참빛-서류 2026 3.32
```

`typeFit` 에 `heir` 가 붙고, 카드는 **가린 근거를 그대로 적는다** —
「2단계 전형이고 이름도 면접형이라 「종합(광운참빛-면접)」으로 이어 읽었습니다」.
근거를 안 적으면 그건 그냥 오연결이다. 적어야 선생님이 아니라고 할 수 있다.

뒤를 잇는 것으로 치는 조건은 좁다 — 같은 카테고리이고, 정규화한 이름이 고른 전형의
이름을 **앞에 그대로 두고 더 붙인 것**이며, 더 나중 해까지 있는 것. 이름이 아주
달라진 경우는 여기서 안 잡히고, 그건 `nearbyOf` 의 몫이다.

그리고 895장 전부에서 **「작년」이라는 이름표를 뗀다.** 「2023년 70% 컷」이라 적는다.
모집요강에서 온 줄(작년 모집인원·실질경쟁률·추가합격)은 정말 2026년 값이라 그대로 둔다.

### 해가 바뀌면서 이름이 바뀐 전형 잇기 (`typeGroups`)

`normType` 으로는 안 붙는 것이 남는다. **글자가 아니라 해로** 알아보는 것들이다.

```
고려대 경제   교과(추천) 2022~2025   →  교과(학교장추천) 2026
가천대 경영   바람개비  2023~2025   →  종합(바람개비)   2026
경상국립대    교과(일반) …           →  교괴(일반전형)   ← 원본 오타
```

같은 해에 둘 다 있으면 둘 다 있는 것이고, 한쪽이 끝난 다음 해에 다른 쪽이 시작하면
이름이 바뀐 것이다. 네 가지를 다 만족할 때만 잇는다.

1. **카테고리가 같다** — 교과와 종합은 애초에 견주지 않는다
2. **해가 안 겹치고 이어진다** — 한쪽 마지막 해 + 1 = 다른 쪽 첫 해
3. **1:1 이다** — 앞도 뒤도 후보가 하나뿐일 때만.
   이게 없으면 강원대 `미래인재` → `미래인재1`·`미래인재2` 처럼 **쪼개진** 것을
   이름이 바뀐 것으로 잘못 읽는다
4. **이름이 0.5 이상 닮았다** (`similarity`)
5. **이름 끝에 번호가 붙은 것이 아니다** — `종합(학생부종합)` → `종합(학생부종합Ⅰ)` 은
   이름이 바뀐 게 아니라 **쪼개진** 것이다. 3의 1:1 만으로는 안 걸린다. 쪼개진 가지
   가운데 하나만 이어지는 해에 나타나면 1:1 로 보이기 때문이다

**문턱 0.5 도 재서 잡았다.** 후보의 닮은 정도 분포에 0.48 과 0.50 사이가 비어 있다.
위쪽은 `교과(일반)→교괴(일반전형)`·`종합(일반)→종합(학생부종합)`·
`교과(추천)→교과(학교장추천)` 처럼 눈으로 봐도 같은 전형이고, 0.40~0.48 은
`교과(지역)→교과(일반)`·`종합(지역)→종합(일반)` 처럼 섞여 있다. 애매한 구간은 안 잇는다.

이어 놓은 것이 맞는지도 쟀다. **같은 이름 한 전형 안에서** 이웃한 해끼리 70%컷이
움직인 폭이 39,055쌍 기준 중앙 0.28 · 95% 1.34 다. 이어 붙인 경계에서의 컷 차이가
1.34 를 넘는 비율은 규칙 5 없이 9%, 넣으면 **6%** — 같은 전형의 이웃한 해에서 기대되는
5% 와 구별되지 않는다. (컷은 확인에만 썼다. 숫자가 이어진다고 이어 붙이면 추이가
실제보다 매끈해진다.)

「면접·서류·교과」 꼴 표시가 다르면 막는 규칙도 재 봤는데 **오히려 나빠졌다**(8%).
좋은 연결을 더 많이 잃는다. 그래서 번호만 본다.

이걸 넣고 「못 가림」이 13.8% → **12.8%** 로 줄었다.

**묶는 일은 `typeGroups` 한 곳에서만 한다.** 카드 머리의 숫자를 고르는 `pickIpgyeol`
과 연도별 추이 표가 같은 함수를 본다. 화면에서 따로 묶으면 표와 머리가 어긋나고,
어긋나도 아무 데도 「어긋났다」고 안 적힌다.

### 작년에 이미 없어진 전형은 올해 전형의 후보가 아니다 (`alive`)

이름으로 못 가리고 유형(교과/종합)이 같은 것도 여럿이면 여태 **아무것도 안 골랐다.**
그런데 후보 가운데 **그 학과의 마지막 해까지 이어진 것이 하나뿐**인 경우가 있다.
나머지는 그 전에 끊긴 전형이라 올해 뽑는 전형의 작년 자리가 될 수 없다.

```
송원대 간호   교과(학생부우수) 2022~2025 → 교과(인성우수) 2026    ← 이어진다
             교과(지역인재)  2024 한 해, 4명                     ← 그해로 끝
2027 모집요강  학생부교과(면접우수자면접전형)
```

`normType` 을 거치면 「교과면접면접」이라 두 이름 어느 쪽과도 글자가 안 겹친다.
`near`(감쌈)도 `sim`(닮음 0.29·0.37)도 못 잡고, 교과가 둘이라 `cat` 도 못 고른다.
그래서 카드에 「지원한 전형을 가려내지 못했습니다」가 붙어 있었다 — 정작
2024에 4명 뽑고 끝난 전형이 후보로 서 있었기 때문이다.

**재서 넣었다.** 2027 모집요강 가운데 입결 학과를 찾은 11,673줄에서 여태 못 가리던
것이 1,323건이다. 이 규칙으로 **80건**이 하나로 좁혀진다(6.0%).

좁힌 답이 맞는지는 **규칙과 무관한 자료로 검산했다** — 모집요강이 저마다 적어 둔
작년 모집인원(모집2026·2025·2024)과 70%컷·50%컷이다. 그 숫자로 판정이 서는 38건이
**38건 다 같은 답**이고 어긋난 것이 없다. 나머지 42건은 모집요강에 검산할 숫자가
없어 확인하지 못했다.

송원대 간호가 그 검산의 본보기다. 모집요강이 적어 둔 모집2027 141 · 모집2026 141 ·
모집2025 58 · 25입결 6.25/6.00 · 24입결 5.50/5.30 이 학생부우수→인성우수 줄과
**전부** 맞고 지역인재(2024·4명)와는 **하나도** 안 맞는다.

안전장치 둘.

1. **`markCut` 이면 안 한다.** 면접·서류가 어긋나 후보를 지운 자리는 옆 전형으로
   메우지 않는다 — 위의 `cat`·`only` 와 같은 규칙이다.
2. **후보가 다 마지막 해까지 이어져 있으면 아무 말도 안 한다.** 이 규칙은 「끊긴
   것을 뺀다」이지 「최근 것을 고른다」가 아니다.

화면에도 이름으로 맞춘 것과 갈라 적는다 — 「이름은 못 맞췄지만 작년까지 이어진
전형이 이것뿐입니다」. 카드 머리에는 `cat`·`only` 와 같은 「전형을 이름으로 맞추지
못했습니다」가 붙고, 학생 화면에는 「(아마도)」가 붙는다.

### 모집요강은 스스로 제 지난 세 해를 적어 두고 있다 (`figures`)

이름으로도 못 가리고 유형으로도 못 좁히고 `alive` 도 못 잡으면 마지막으로 두드리는
곳이다. **모집요강 한 줄은 저마다 제 지난 세 해를 적고 있다** — 모집2026·2025·2024
와 그 해의 70%컷·50%컷. 그건 대학이 「이 전형의 작년은 이것이었다」고 직접 말한
것이라, 그 사이에 이름이 바뀌었든 말든 그대로 쓸 수 있다.

```
경성대 경영  모집요강 「학생부교과(일반계고교과전형)」
             26  60명 4.00 / 3.90     25  60명 4.00 / 3.80     24  60명 4.20
입결        교과(일반계교과)  26 60명 4.00/3.90 · 25 60명 4.00/3.80 · 24 60명 4.20  ← 여덟 개 다 맞음
            교과(일반계면접)  26 27명 4.00/3.90 · 25 27명 4.60/4.40 · 24 28명 4.60  ← 둘만 맞음
```

**가드 없이는 못 쓴다.** 이름이 이미 확실히 붙은(`exact`) 자리에 이 규칙을 돌려
답을 견주는 식으로, 조여 가며 쟀다.

| 가드 | 판정 선 건수 | 이름과 어긋남 |
|---|---|---|
| 없음 | 6,587 | 132 (2.00%) |
| + 유형(교과/종합/논술/실기)이 같을 것 | 4,985 | 120 (2.41%) |
| + 쪼개진 형제가 보이면 물러남 | 4,180 | 19 (0.45%) |
| **+ 2등과 2점 이상 벌어질 것** | **4,108** | **1 (0.02%)** |

「종류 둘」·「3점 이상」·「해 둘」도 재 봤는데, 어긋남은 더 안 줄고 좁히는 건수만
깎였다. **더 조여도 얻는 게 없으면 조이지 않는다.**

남은 하나는 규칙의 실패가 아니라 **자료가 얽힌 자리**다. 초당대 간호는
「일반고(~2025, 127→135명)」가 2026에 「일반학생(100명)」으로 이름을 바꾸면서
**새 「일반고」(70명)가 따로 생겼다.** 모집요강 「일반학생전형」의 26 은 새 이름을,
25·24 는 옛 이름을 가리키니 이름 답과 숫자 답이 둘 다 반쯤 맞다. 그리고 그 자리는
이름이 이미 붙는 곳이라 이 갈래가 애초에 닿지 않는다.

**어긋난 예가 무엇을 가르쳤나** — 가드 하나하나가 실제로 나온 오연결을 막는다.

```
경희대 의상 논술(논술우수자)  → 교과(지역균형)      유형 가드가 막는다
강남대 디자인 종합(학교생활Ⅱ) → 종합(서류면접)      형제 가드가 막는다
                              (부모가 사라지고 Ⅰ·Ⅱ 만 남아 isSplit 이 못 잡는다.
                               꼬리 번호를 뗀 줄기로 형제를 알아본다)
초당대 간호 교과(일반학생)    → 교과(일반고)        2점차 가드가 막는다
```

가드 넷을 다 건다.

1. **모집요강 줄을 이름으로 맞췄을 때만**(`pickMojip` 의 `byName`). 아니면 옆 전형의
   지난 세 해를 이 전형의 것인 양 읽어 숫자가 통째로 오염된다
2. **유형이 같은 후보만** 센다
3. **쪼개진 흔적이 보이면 물러난다** — 번호 꼬리가 붙은 이름이 후보에 있거나 둘이
   형제면 아무 말도 안 한다
4. **2점 이상 맞고 2등과 2점 이상 벌어질 것**

기준 칸도 반드시 함께 본다. 「70%컷(최종)」만 등급이고 「평균(최종)」·「80%컷」·
「최저(최종)」은 다른 잣대다(2025 기준 칸만 여섯 가지). **「환산70%컷」은 등급이
아니라 점수라 뺀다** — 안 빼면 700 과 5.6 을 견주게 된다.

이걸 넣고 못 가리는 것이 **1,243 → 1,020건**이 됐다(223건). `alive` 와 합치면
1,323 → 1,020, **23%가 줄었다.**

### 올해 신설이면 옆 전형의 컷을 이 전형의 것인 양 달지 않는다

모집요강이 작년 모집인원과 경쟁률을 둘 다 비워 두면 올해 처음 뽑는 것이다(12.2%).
그런데 이름이 닮았다는 이유로 옆 전형에 붙어 그 컷을 달고 나오는 일이 있었다 —
신설 2,205건 가운데 **572건**이 그랬다.

```
건국대(글로컬) 의예과  「지역의사제(광역권)」  →  교과(지역인재) 의 1.35 를 달고 나옴
```

작년에 없던 전형이니 작년 컷도 없는 게 맞다. 그래서 머리 숫자에서 빼고 **「참고 —
이 학과의 관련 전형」**으로 옮긴다. 자유전공의 「묶이기 전 학과 참고」와 같은 자리,
같은 꼴이다. 두 갈래로 낸다.

| | 무엇을 곁들이나 |
|---|---|
| 하나로 좁혀졌으면 | 그 전형만 (「지역의사제」 ↔ 「지역인재」) |
| 못 좁혔으면 | **같은 유형의 전형을 전부, 범위로** |

둘째가 오히려 흔하고 그게 맞다. 올해 처음 뽑는 전형이면 작년에 짝지을 것이 애초에
없는 게 정상이다. 경성대 영어영문 「교과(지역인재)」가 그렇다 — 작년 교과는
「교과(일반계교과)」 4.30 과 「교과(일반계면접)」 4.70 이었다. 하나를 골라 주는
것보다 둘 다 보여 주고 「이 학과 교과는 이 언저리」라고 말하는 편이 정직하다.

**유형이 다르면 참고로도 안 준다.** 논술 신설에 교과 컷을 참고랍시고 보여 주면
그건 참고가 아니라 헛말이다.

같은 까닭으로 `only` 갈래에도 유형 조건을 걸었다. 하나뿐이라는 것은 고를 게 없다는
뜻이지 그게 맞다는 뜻이 아니다 — 동국대 바이오시스템 논술에 교과(학교장추천) 1.30 이,
광주대 태권도 교과에 실기실적 7.38 이 붙고 있었다. 재 보니 `only` 로 도달하던 38건은
**전부** 유형이 어긋난 것이었다. 유형이 맞으면 위의 `cat` 갈래가 이미 잡는다.

### 컷을 못 채우는 까닭이 넷이다 — 뭉치면 다 「자료 없음」이 된다

학과가 붙은 모집요강 줄 14,454개를 기준으로 재면 이렇다.

| | | 화면이 하는 말 |
|---|---|---|
| 컷이 붙음 | 73.4% | 숫자 |

> 아래 표는 `alive`·`figures` 를 넣기 **전**에 잰 것이다. 그 뒤 「못 가림」이
> 1,323 → 1,020건으로 줄었다 — 위 두 절 참고.
| 관련 전형 참고 (신설) | 6.2% | 「올해 신설이라 작년 값이 없습니다」 + 같은 유형 범위 |
| 관련 전형 참고 (못 가림) | 5.9% | 「어느 전형인지 가려내지 못했습니다」 + 같은 유형 범위 |
| 논술 입결이 없음 | 5.0% | 「논술은 공개하는 대학이 적습니다」 |
| 컷 칸이 비어 있음 | 7.6% | 「줄은 있는데 70%컷 칸이 비어 있습니다」 |
| 교과·종합 입결이 없음 | 2.0% | 「이 학과에 ○○ 전형 입결이 없습니다」 |

넷 다 「작년 컷 없음」으로 뭉쳐 두면, 논술 지원자가 「자료 정리가 덜 됐나 보다」로
읽는다. 실제로는 **논술 결과를 공개하는 대학이 적어서** 없는 것이다 — 입결 75,200행
가운데 논술은 966행뿐이고 그중 70%컷이 적힌 것은 46% 다. 더 찾아봐도 안 나온다.

**가장 최근 해에 컷이 없으면 그 앞 해를 본다.** 2026 줄은 있는데 70%컷 칸만 비어
있는 경우가 흔해서, 예전에는 2022~2025 네 해치를 갖고도 카드가 통째로 빈칸이 됐다
(강원대 교육학과 교과(지역교과) 등 521건). 컷이 있는 가장 최근 줄을 **통째로** 쓰고
어느 해 것인지 화면에 적는다 — 칸마다 다른 해에서 긁어모으면 경쟁률은 2026, 컷은
2025 인 카드가 되고 그건 어느 해 이야기도 아니다.

**등급이 범위 밖이면 없는 것으로 친다.** 원본에 70%컷이 `0` 으로 적힌 줄이 9개 있다.
등급에 0 은 없다 — 못 구했다는 뜻으로 넣은 것이다. 그대로 두면 화면에 `0.00` 으로
찍히고 그건 **만점처럼 읽힌다.**

### 학과 이름이 바뀐 것은 **찾아 주기만 하고 잇지 않는다**

학과는 자주 바뀐다. (대학·학과) 8,502 곳 가운데 **30.4%가 2026에 없고 23.6%가
2022에 없다.** 대학이 학부를 쪼개고 합치고 이름을 갈아 대기 때문이다. 그래서 올해
원서를 쓰는 학과가 입결에는 한두 해치밖에 없는 일이 흔하다 — 모집요강 학과 833곳.

그중 145곳은 **바로 앞 해에 끝난 이름 닮은 학과**가 1:1로 있다.

```
강원대  일본학과(2026)        ←  일본어학과(~2025, 4해)
강원대  수의학과(2026)        ←  수의예과(~2025, 4해)
가천대  건설환경공학과(2026)   ←  토목환경공학과(~2025, 3해)
```

**그래도 자동으로 잇지 않는다.** 재 봤다 — 같은 학과 안에서 이웃한 해의 70%컷이
움직인 폭이 19,345쌍 기준 중앙 0.25 · 95% 1.13 인데, 이렇게 이어 붙인 경계에서
1.13 을 넘는 비율이 **14%** 다. 기대치 5%의 세 배고, 문턱을 0.8로 올리면 오히려
17%로 나빠진다.

전형 이름과는 다른 이야기다. 전형은 이름만 갈리는 일이 많아 숫자 꼬리만 막으면 6%로
떨어졌다. 학과는 **이름이 바뀔 때 가르치는 것과 뽑는 수도 같이 바뀐다.**
「수의예과 → 수의학과」는 같은 곳이지만 「기계공학과 → 기계융합공학부」는 다를 수
있고, 자료만 보고는 못 가린다.

그래서 카드가 알려 주기만 한다 — 「이 학과는 입결에 1해치뿐입니다. 같은 대학에
「일본어학과」가 2025까지 4해 있었습니다」. 이을지는 선생님이 점검 화면에서 정한다.

**가운뎃점이 네 벌 섞여 있었다** — `·`(U+00B7) 1,740 · `・`(U+30FB) 1,733 ·
`ㆍ`(U+318D) 356 · `‧`(U+2027) 147. `normDept` 가 `ㆍ` 를 안 털어서 강원대
「원예ㆍ농업자원경제학부」와 「원예·농업자원경제학부」가 서로 다른 학과가 되어 있었다.
한 학과의 자료가 두 곳에 나뉜 채 각각 몇 해씩만 남는다. 14쌍이 그랬다.
`scripts/build_xref.py` 의 `norm_dept` 도 같이 고쳤다 — **둘은 같아야 한다.**

### 대학 이름 꼬리 「~대학」 — 전문대가 여기서 갈렸다

즐겨찾기는 「명지전문대학」「인하공업전문대학」이라 적고 전문대 자료는
「명지전문대」「인하공업전문대」다. `univStem` 이 「대학교」 꼬리만 털어서
이 지원들이 「자료에 대학을 찾지 못했습니다」로 남았다 — 자료에는 그
학과(항공서비스과·항공운항과)가 그대로 있는데도.

「대학$ → 대」 규칙을 더했다. 넣기 전에 네 자료(입결·모집요강·전문대·
즐겨찾기)의 실이름 450개 전체로 재서 **서로 다른 학교가 새로 합쳐지는
일이 없음**(0건)을 확인했다. 꼬리를 턴 뒤 축약 규칙(여자대→여대 등)도
그대로 이어진다 — 「한양여자대학」이 「한양여대」에 닿는다.

### 자유전공 · 학과통합

모집요강의 `[유형1]` · `[유형2]` 꼬리표가 붙은 모집단위는 세부모집단위가 잘게 나눈
이름이 아니라 **묶이기 전 학과 목록**이다. 그걸로 작년 선을 그린다.
유형2 모집단위 136곳 중 81곳에서 그릴 수 있다.

평균 하나로 줄이지 않고 범위·가운데값·학과별 값을 다 준다. 묶고 나면 경쟁률도 컷도
달라지므로 한 숫자로 말할 수 있는 자료가 아니다.

---

## 6. 숫자를 다루는 규칙

### 차이값을 계산하지 않는다

전교과 등급과 대학 환산 등급은 같은 잣대가 아니다. 「여유 0.24」처럼 빼는 순간
자료가 말하지 않는 것을 말하게 된다. 숫자를 그대로 두고 판단은 사람이 한다.

카드에서 대학 쪽과 학생 쪽을 세로 선으로 갈라 놓은 것도 같은 까닭이다.

### 유의검정을 하지 않는다 — 그러나 어림은 한다. 갈라 놓는다

입결은 대학이 공개한 **요약값**이다 — 70%컷 하나, 50%컷 하나, 경쟁률 하나.
합격자 개개인의 점수가 없다. 개별 값이 없으면 분산을 모르고, 분산을 모르면 검정도
신뢰구간도 만들 수 없다. p값을 적으면 그건 계산이 아니라 지어내기다.
**그건 지금도 안 한다.**

먼저 정말 **센 값**을 낸다 (`board/confidence.js`) — 해마다 얼마나 움직였나,
몇 명을 뽑나, 어느 쪽으로 가고 있나, 두 컷 사이 어디쯤인가.

그 **다음에** 어림한 값을 낸다. 등급50(가운데)과 등급70(상위 70% 지점)은 합격자
분포의 두 분위점이다. 정규근사로

```
μ = 등급50,  σ = (등급70 − 등급50) ÷ 0.524
내 자리 = Φ((내 등급 − μ) ÷ σ)
```

를 잡아 「작년 합격자 중 상위 몇 %」와 안정·적정·소신·상향을 낸다. 올해 컷도
직전값과 가중평균을 절반씩 섞고 급변한 해는 절반쯤 되돌려 **범위로** 낸다.

이 식·문턱·한계 문구는 이 저장소의 `counsel.html` 이 47,128건으로 타당성을 재고
쓰던 것을 **그대로** 가져온 것이다(등급70−등급50 차이의 중앙값 0.20, IQR 0.09~0.39).
두 도구가 같은 자료를 두고 다른 말을 하면 안 된다. **한쪽을 고치면 다른 쪽도 고친다.**

지켜야 할 세 가지.

1. **「합격 확률」이라 부르지 않는다.** 이건 *작년 합격자들 사이에서 내가 어디쯤인가*
   이지 올해 붙을 확률이 아니다. 화면 문구도 그렇게 쓴다.
2. **잰 값과 갈라 놓는다.** 화면에서 왼쪽 선으로 구역을 나누고 「어림해 보면」이라
   적는다. 같은 표에 섞으면 어디까지가 자료이고 어디부터가 가정인지 구별이 안 된다.
3. **한계를 함께 적는다.** ① 최종등록자 기준이라 추가합격 막차는 더 낮다
   ② 정규분포는 근사라 적게 뽑는 학과는 오차가 크다 ③ 대학별 반영교과 차이는 안 들어
   있다. 「출발점이지 결론이 아니다」를 화면에 그대로 적는다.

잰 값과 어림한 값이 **다르게 보일 수 있다.** 「내 위치」는 가장 최근 한 해의 두 컷만
보고, 「어림해 보면」은 최근 두 해를 평균 낸다. 어긋난 게 아니라 잣대가 다른 것이라,
양쪽에 어느 해를 보고 한 말인지 적어 둔다. 안 적으면 둘 다 못 믿게 된다.

### 환산이 없는 지원은 전교과로 대신 잰다 — 잣대를 적고서

관심대학 리스트의 내등급은 전형별 환산이라 **종합전형은 0(=없음)으로 온다**
(Code.gs `score_` 가 0 을 「안 적음」으로 거른다). 그 지원은 위치 어림이 통째로
비었다. 그래서 선생님이 학년 전체의 전교과 일반등급을 시트 「성적」 탭에 붙여
두면(`parseGradeRows_` — 머리글을 자리로 집지 않고 이름으로 찾는다), store 가

1. 즐겨찾기에 전교과가 없는 학생의 `naesin['전교과']` 를 채우고 (출처를 남긴다)
2. 셈에 쓸 등급 하나를 `gradeOf` 가 고른다 — 환산이 있으면 환산, 없으면 전교과.
   **잣대(scale)를 함께 돌려주고 화면이 반드시 적는다.** 전교과는 대학 반영교과
   환산과 다른 잣대라, 안 적으면 어림이 잰 값처럼 읽힌다(§6 「차이값을 계산하지
   않는다」와 같은 감각이다 — 값을 대신 쓸 수는 있어도 말없이 쓰면 안 된다)

**학번이 같은데 이름이 다르면 붙이지 않는다.** 반이 바뀌었거나 번호가 밀린
것일 수 있고, 남의 성적으로 위치를 재는 것이 빈 것보다 나쁘다. 점검 화면이
그 학번을 표로 보여 준다(즐겨찾기 이름 · 성적 탭 이름).

곁들여 잡은 실버그 — `Number(null)` 은 0 이라, myScore 가 빈 지원이 검토판
환산 칸에 **0.00** 으로 찍히고(1등급보다 좋은 성적으로 읽힌다) 학과 줄의 환산
범위 아래끝을 0.00 으로 끌어내리고 있었다. 등급에 0 은 없다 — `> 0` 으로 거른다.

### 학과 탐색의 가상 분포 그림 — 그릴 만한지 재고 넣었다

표의 「70%컷 5.60」 옆에 학생의 「5.45」가 서 있으면, 그 사이가 얼마나 되는 자리인지
안 읽힌다. 폭이 0.1인 전형과 0.9인 전형에서 같은 0.15 차이가 전혀 다른 뜻인데
표는 그 말을 못 한다. 그래서 학과 검토판에 **전형별 곡선 한 장**을 접어 두었다
(`explore.js` `distBlock`). 곡선은 위의 μ·σ 식 그대로고, `confidence.js` 의
`fitCurve` **한 곳**에서만 잡는다 — 그림과 카드의 백분위가 어긋나면 안 된다.

넣기 전에 두 가지를 쟀다(입결 75,592행).

**1. 모양을 몰라도 되는가 — 된다.** 두 분위점에 정규분포 대신 로지스틱·t(5)를
맞춰 보면(같은 두 컷을 지나게 하고, σ 한계도 같은 폭으로 옮겨 맞춘 뒤 학생 자리를
50%컷 −1.0 ~ +1.5 에 걸쳐 재면) 안정/적정/소신/상향 판정이 갈리는 것이 각각
**0.7% · 1.2%**, 백분위는 중앙 0.2~0.3%p(95% 1.5~2.0%p)밖에 안 움직인다.
두 분위점을 지나는 곡선은 어떤 모양을 골라도 그 언저리를 지난다.
**모양은 약한 고리가 아니다.**

**2. 약한 고리는 해다.** 같은 전형의 작년 곡선 대신 재작년 곡선으로 재면 백분위가
중앙 **13.1%p**(75% 28.4 · 95% 55.3) 움직이고 판정은 **45.1%** 가 갈린다(이웃한 두
해가 다 있는 31,163쌍). 모양을 바꾼 것보다 **40배** 크다. 그래서 매끈한 곡선 하나로
그리지 않는다 — **해마다의 곡선을 옅게 겹쳐** 그려서 이 그림이 해마다 얼마나
달라지는지를 같이 보인다.

**그리고 잰 데와 가정한 데를 선으로 가른다.** 두 컷 사이만 실선·앰버 면이고 그
밖은 점선·회색 면이다. 이게 중요한 까닭 — 학생이 두 컷 사이에 서는 일이 드물다.
50%컷보다 0.5등급 아래면 **17.9%**, 1등급 아래면 **4.8%** 뿐이다. **이 그림에서
학생은 대개 가정한 쪽에 선다.** 안 가르면 가정을 자료로 읽는다.

못 그리는 경우도 말한다.

| | 얼마나 | 화면이 하는 말 |
|---|---|---|
| 두 컷이 다 있고 폭이 양수 | 행 74.5% · (대학·학과·전형) 조합 80.0% | 그린다 |
| 두 컷이 **똑같이** 적힘 | 두 컷 있는 줄의 12.1% (7,728) | 「두 컷이 거의 붙어 있어 폭은 자료가 아니라 한계값(0.18)입니다」 |
| 70%컷이 비어 있음 | — | 「70%컷이 비어 있어 그릴 수 없습니다」 |
| 전형을 못 가림 · 올해 신설 | — | 그 까닭을 그대로 적는다 |

σ 한계에 걸리는 것도 드물지 않다 — 폭이 양수인 56,300줄 중 **25.8%가 하한,
7.7%가 상한**이다. 폭이 자료가 아니라 한계값에서 온 것이면 그렇다고 적는다
(`fitCurve` 의 `clamped`).

**표를 밀어내지 않게 접어 둔다.** 상담에서 읽는 것은 숫자다(§7). 그림은 숫자를
읽기 전에 자리를 잡아 주는 것이지 숫자를 대신하는 것이 아니다. 「합격 확률」이라
부르지 않는 것, 한계 셋을 함께 적는 것은 위와 같다.

### 문턱은 짐작이 아니라 자료를 재서 잡는다

「많이 흔들림」의 문턱을 처음에 0.5등급으로 잡았더니 절반이 걸렸다. 실제 분포를 재니
움직인 폭의 중앙값이 0.54였다. **흔한 것을 경고하면 경고가 사라진다.**
상위 10% 인 1.5등급으로 다시 잡았다.

같은 이유로 모집인원 경고를 15명 이하에서 3명 이하로 좁혔다 — 모집 15명 이하가
전체의 76% 다. 그게 보통이지 예외가 아니다.

### 「흔들린다」와 「모른다」를 가른다

그 전형의 입결이 한 해치뿐인 경우가 61% 다. 그건 불안정하다는 뜻이 아니라 **아직
모른다**는 뜻이다. 섞어 세면 절반 넘는 카드에 경고가 붙고, 정말 흔들리는 16% 가 묻힌다.

### 실질경쟁률

```
실질경쟁률 = 명목경쟁률 × 모집인원 ÷ (모집인원 + 추가합격인원)
```

모집+추합이 지원자보다 많으면 추합란에 최종 인원이 아니라 누적 예비번호가 적힌
것이므로 **계산하지 않고 사유를 적는다.** 억지로 계산하면 1 미만이 나와 미달처럼 보인다.

---

## 7. 화면 규칙

`web-ui-standard` 를 따른다. 요약하면

- 액센트는 **앰버 하나**. 의미색은 오류 하나만.
- **붉은색은 진짜 문제에만.** 두어 명 줄어든 모집인원까지 붉게 칠하면 정작 반 토막
  난 전형이 눈에 안 들어온다.
- 표와 숫자가 주역. 그래프는 상담에서 읽는 것이 아니다.
- `word-break: keep-all` · `tabular-nums` · 한두 글자만 다음 줄로 넘어가지 않게
- 입력칸은 16px 이상 (그 아래면 iOS 가 화면을 확대한다)
- **격자 칸은 `min-width: 0`** 을 준다. 기본값 `auto` 라서 안의 넓은 표가 칸을 밀어
  화면 밖으로 나간다. 표는 제 안에서(`.tw`) 옆으로 넘겨야 한다.
- `@media print` 를 처음부터 함께 짠다. 진학 대장은 학생 한 명이 한 쪽이다.

확인은 **320 · 390 · 768 · 1280 · 1440** 다섯 폭에서. 가로 스크롤 0, 넘친 요소 0,
콘솔 오류 0. 넓은 것(표·일정·내보내기 미리보기)은 제 칸 안에서 가로로 굴러야 하고
본문이 굴러서는 안 된다.

### 좁은 폭에서 학과 검토판이 본문을 밀고 있었다

`.row` 는 480px 아래에서 한 칸짜리 격자인데, 칸 크기를 안 정해 둬서 기본값
`minmax(auto, 1fr)` 이 됐다. `auto` 는 안에 든 것 중 가장 넓은 것을 따라간다.
학과 검토판을 펴면 전형별 자료 표가 그 칸을 밀어 **본문이 통째로 옆으로 굴렀다**
(390px 에서 `scrollWidth` 672). 표는 제 안에서(`.tw`) 굴러야 한다.

`grid-template-columns: minmax(0, 1fr)` 한 줄이다. §7 이 이미 적어 둔
「격자 칸은 `min-width: 0`」인데, `.row .txt` 에만 있고 격자 칸 자체에는 없었다.
다섯 폭(320·390·768·1280·1440)에서 다시 재서 가로 스크롤 0 · 넘친 요소 0 이다.

### 종이는 화면과 따로 잰다

내보내기 문서는 **A4 폭에서** 재야 한다. 화면(1280px)에서 멀쩡하던 칸이 A4 세로
186mm 에서 두 줄로 갈린다. 브라우저에 `print` 매체를 씌우고 `.sheet` 폭을
186mm(세로) · 286mm(가로)로 박은 뒤, 칸마다 줄 상자 수를 세고 `scrollWidth` 로
넘쳤는지 본다. 일부러 여러 줄로 짠 칸(등록 대학의 대학/학과, 메모 목록, 접어 둔
대학 이름 줄)은 뺀다.

이렇게 재서 고쳤다.

| | 갈린 칸 (전) | (후) | 쪽수 (전 → 후) |
|---|---|---|---|
| 진학 대장 | 5 | **0** | 1 → 1 |
| 지원 결과 보고서 | 8 | **0** | 5 → 5 |
| 합격자 발표 현황 | 5 | **0** | 2 → **1** |
| 최종 결과 보고서 | 9 | **0** | 5 → **3** |

쪽수는 보기용 자료(4명·26건) 기준이라 실제로는 더 줄어든다 — 최종 결과 보고서의
대학 명단은 학생 수와 무관하게 85줄이 고정으로 붙어 있었다.

명단을 대학별로 묶으면서 대학 칸이 머리줄로 빠져, 가로로 돌렸던 것을 다시 세로로
되돌렸다. 지금은 진학 대장만 가로다 — 한 문서 안에서 방향이 바뀌면 인쇄해 놓고
넘길 때 손이 걸린다.

그 묶기에서 **오래된 셈 오류 하나가 드러났다.** 즐겨찾기는 「연세대학교(서울)」로
주는데 예년 명단은 「연세대학교」다. 이름이 안 맞아 예년 명단의 그 줄은 0으로 남고
옆에 「연세대학교(서울)」이 따로 생겼다. 지원이 없는 줄을 접고 나서야 보였다 —
지원 1건이 있는데도 「지원 없음」 목록에 연세대학교·고려대학교가 들어 있었다.
`univOrder` 가 이름 그대로 → 괄호를 뗀 이름 차례로 맞춘다(`rankTable` 이 이미
쓰던 방법이다). 「한양대학교(ERICA)」가 「한양대학교」에 딸려 들어가지 않도록
**차례를 지키는 것**이 핵심이다.

이 재기는 `scripts/check_print.mjs` 에 넣어 뒀다. 서식을 손대면 다시 돌린다.

```
python3 -m http.server 8899 &
node scripts/check_print.mjs
```

고친 것은 `board/CONTRACT.md` §5.8 「한 칸은 한 줄로」에 적어 두었다.

---

## 8. 시험

```
sh scripts/check_syntax.sh     # 구문 (board/*.js 는 ESM 이라 .mjs 로 복사해 잰다)
node board/parse.test.mjs      # 엑셀 파서 (Code.gs 를 node 에서 그대로 돌린다)
node board/match.test.mjs      # 이름 맞추기
node board/stats.test.mjs      # 결과 통계
node board/confidence.test.mjs # 얼마나 믿을 수 있나
node board/auth.test.mjs       # 누가 무엇을 부를 수 있나 (Code.gs 를 시트 대역과 함께)

python3 -m http.server 8899 &          # 아래 둘은 브라우저가 필요하다
node scripts/check_print.mjs           # 내보내기 문서를 A4 폭에서 (갈린 칸 0 이어야 한다)
```

`parse.test.mjs` 는 `Code.gs` 를 `new Function` 으로 불러 **실제로 배포되는 코드**를
돌린다. 파서를 두 벌 두면 반드시 갈라지기 때문이다.

`check_syntax.sh` 는 `.mjs` 로 복사해서 잰다. `node --check board/board.js` 로는
확장자가 `.js` 라 node 가 CommonJS 로 읽고 조용히 넘어가는 일이 있다 — 실제로
중괄호가 하나 빠진 채 통과하고 브라우저에서야 드러났다.

`parse` 와 `match` 는 실제 즐겨찾기 파일을 넘기면 훨씬 많은 것을 본다.
**그 파일은 저장소에 넣지 않는다.**

```
node board/parse.test.mjs /경로/즐겨찾기.xlsx
```

CI 는 없다. 사람이 돌린다.

---

## 9. 고칠 때 지킬 것

- **못 붙인 것을 숨기지 않는다.** 추측으로 채우느니 「연결 안 됨」이 낫다.
- **원문을 버리지 않는다.** 못 읽은 칸은 `unknown` 에 남겨 화면에 띄운다.
- **어느 자료에서 온 값인지 적는다.** 상세의 줄마다 출처가 붙어 있다.
- **모르는 표기를 조용히 세지 않는다.** 결과 통계는 못 알아본 값을 따로 돌려준다.
- **만든 화면에 가는 길을 둔다.** 반 전체 일정판은 만들어 놓고도 학생을 한 번 고르면
  돌아갈 길이 없어 반년 동안 아무도 못 볼 뻔했다.
- **일은 그 일을 아는 사람에게 준다.** 결과 입력을 선생님 화면에 뒀다가 학생 쪽으로
  옮겼다. 121명 × 여섯 칸이면 700건이고, 합격자 발표는 학생이 먼저 본다.
  선생님은 「맞습니다」만 누른다.
- **해야 할 일이 첫 화면에 있어야 한다.** 학생이 밤사이 적어 둔 것을 담임이 알려면
  명단을 한 명씩 열어 봐야 했다.
- **경고를 뭉쳐 세지 않는다.** 확정 겹침 한 건이 「겹칠 수 있음」 넷에 묻히면
  겁만 주고 판단은 못 돕는다.
- **자료 쪽 표기를 글자 그대로 믿지 않는다.** 학교유형이 「대학교」로 적혀 있다고
  여섯 칸을 통째로 미연결로 만들면 안 된다. 못 알아본 말은 못 알아봤다고 두고
  두 자료를 다 뒤진다 (`univKind`).
- **못 읽었으면 옛것으로 대신하지 않는다.** 딴 파일의 원본을 못 열었을 때 조용히
  이 시트를 읽으면 지난주 자료가 이번 주 자료 행세를 한다.
- **화면 이름과 CSS 이름이 겹치는 것을 조심한다.** 범례의 「발표」 칸에 `note` 를 썼더니
  전역 `.note`(알림 상자)의 padding 을 물려받아 그 칸만 26px 로 부풀었다.
  이름이 겹치는 것만으로 조용히 어긋난다.
- **글자로 결과를 세지 않는다.** 「1단계 합격」에는 「합격」이 들어 있고 「미등록」에는
  「등록」이 들어 있다. 무엇을 뜻하는 말인지 표로 정해 두고, 그 표만 본다.
- **한 사건에 쓰기가 둘이면 뒤가 실패했을 때 앞을 되돌린다.** 반쯤 옮겨진 채로 두느니
  아무것도 안 옮긴 편이 낫다 (순위 맞바꾸기).
- **입결이 없다와 모집요강이 없다는 다른 사실이다.** 한쪽이 없다고 다른 쪽까지 버리면,
  자료가 제일 필요한 카드(신설학과)가 제일 빈 카드가 된다.
- **그럴듯한 숫자를 제일 조심한다.** 학과만 맞추고 전형은 아무 줄이나 쓴 컷은
  73% 의 카드에서 틀렸는데도 아무 데도 「틀렸다」고 안 적혀 있었다. 자리가 비어
  보이는 것보다 채워져 보이는 것이 훨씬 오래 안 들킨다.
- **묶어 놓은 것은 무엇으로 묶었는지 적는다.** 연도별 추이가 이름이 다른 두 전형을
  한 묶음으로 보여 주면 「예전 이름 …」을 함께 적는다. 안 적으면 왜 한 묶음인지
  알 수 없고, 자료가 틀린 건지 화면이 틀린 건지도 가릴 수 없다.
- 모든 사용자용 문장은 한국어. 단정할 수 없으면 단정하지 않는다.
- **이 문서와 코드를 같이 고친다.**
