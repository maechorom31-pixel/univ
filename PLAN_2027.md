# 2027 수시 전형설계 검색기 — 자료 검토 및 제작 계획서

> 작성: 2026-08-12 · 원본: 2027학년도 수시 모집요강 요약본 엑셀(김영일교육컨설팅, 2026-07-23 기준)
> 이 문서는 설계·검토 단계 산출물이며, 실제 구현 작업의 지시서로 사용한다.

---

## 1. 자료 개요

| 항목 | 내용 |
|---|---|
| 원본 시트 | `2027모집요강` (헤더 4행, 데이터 5행~) |
| 원시 행수 | 16,978행 → 정제 후 **16,873행** (완전중복 및 입력 글리치 105행 제거) |
| 대학 수 | **223개교** · 17개 지역 |
| 계열 | 자연 9,183 · 인문 6,527 · 예체능 790 · 공통 478 |
| 세부전형 | 534종 (교과/종합/논술/실기 계열) |
| 정제 산출물 | `data/mojip2027.json` (4.6MB, 문자열 사전 방식으로 중복 제거) |
| 재생성 | `python3 scripts/parse_2027.py <엑셀경로> data/mojip2027.json` |

### 컬럼 구성 (55개)

- **식별**: 지역, 대학명, 세부전형, 계열, 모집단위, 세부모집단위
- **모집인원 4개년**: 2024~2027 (연도별 증감 추이 산출 가능)
- **경쟁률 3개년**: 2024~2026
- **지원자격**: 자격학년도(졸업연도 제한 — `제한없음`/`2027`/`2025`… ), 지원자격 전문
- **전형방법**: 전형단계(1/2), 단계별 선발비율·전형방법 텍스트
- **학생부 반영**: 학년별 반영비율(1/2/3/1,2/2,3/전체), 교과·출결·봉사 비율, 공통·일반선택 반영, 진로선택 반영, 전형총점, 반영총점
- **수능최저**: 전문 텍스트 (5,795행 보유 = 34%)
- **일정**: 대학고사 종류(면접 3,882 / 논술 1,307), 고사시기, 1단계 발표일, 논술일정, 면접일정, 합격자발표일
- **3개년 입결**: 연도별 입결1(대부분 70%컷)·입결2(대부분 50%컷)·**추가합격 인원**

### 데이터 커버리지 (16,873행 기준)

| 항목 | 보유 행수 |
|---|---|
| 2026 경쟁률 | 14,874 |
| 2026 입결1(70%컷) | 12,675 (100% 숫자 파싱 성공) |
| 2026 추합 인원 | 12,934 |
| **실질경쟁률 계산 가능(2026)** | **12,773행** |
| 실질경쟁률 계산 가능(2025 / 2024) | 11,126 / 10,542 |
| 수능최저 텍스트 | 5,795 |

---

## 2. 검증 결과

전반적으로 **잘 정제된 자료**다. 다만 숫자 칸에 텍스트가 섞인 곳과 원자료 내부 모순이 있어, 그대로 계산에 넣으면 통계가 깨진다. 아래는 모두 파서·뷰어에서 처리를 끝냈다.

1. **중복 글리치 (제거 완료)**: 홍익대·홍익대(세종)·목포대·화성의과학대·부산대(양산)에서 동일 행이 이중 입력됨(내용 동일, `입결1기준`만 `70%컷` vs `0` 표기 차이). 결측이 적은 행을 남기고 105행 제거 → 16,873행.
2. **숫자 칸에 섞인 텍스트 (정제 완료)** — 이걸 그대로 두면 모집인원 합계가 문자열로 이어붙어 통계가 무너진다.
   - `통합(10)` 형태(계열통합 모집, 62건) → 괄호 안 숫자를 모집인원으로 사용
   - `미발표`(경쟁2024, 40건) → 결측 처리
   - `2027 신설` / `2025 통합 모집` (입결 값 칸, 약 4,900건) → 결측 처리 후 **`특이표기` 컬럼**으로 분리 수집
   - `5..38`(마침표 중복 오타) → 5.38로 교정 · `2 6`, `2.00 ~ 6.00` 등 6건은 판독 불가로 결측
3. **입결 기준 혼재**: `입결1기준`이 대부분 `70%컷(최종)`이지만 `평균(최종)` 545행, **`환산70%컷`은 등급이 아니라 환산점수(예: 994.9)**. 뷰어는 기준 문자열에 `환산`이 있거나 값이 1~9 밖이면 등급 축에서 제외한다. (검증: 등급 축에 오른 13,725개 값 전부 1.0~9.0 범위)
4. **50%컷 > 70%컷 역전 342행(2.9%)**: 원본 입력 편차. 상세 모달에 기준 라벨을 그대로 병기해 사용자가 판단하게 했다.
5. **★ 추가합격 인원과 경쟁률의 모순 332건(0.97%)**: `모집인원 + 추가합격`이 `지원자 수(경쟁률 × 모집인원)`보다 많은 행이 있다. 예) 가톨릭관동대 경영학전공 2024 — 모집 24, 경쟁률 1.5(지원 36명)인데 추합 36명. **추가합격이 순증 인원이 아니라 누적 예비번호로 기재된 것으로 보인다.** 이런 행에서 실질경쟁률 공식을 그대로 쓰면 0.14 같은 무의미한 값이 나오므로, 뷰어는 **계산에서 제외하고 상세에 사유를 밝힌다**. (경쟁률이 소수 둘째 자리 반올림인 점을 감안해 여유를 두고 판정)
6. **미달 학과 54건**(명목경쟁률 < 1): 추가합격 개념이 성립하지 않으므로 실질경쟁률 = 명목경쟁률로 둔다.
7. **NO 컬럼은 무의미** (내부 일련번호 혼재) → 파서에서 제외함.

---

## 3. 이 자료만의 가치 (기존 입결검색기 대비)

기존 `index.html`은 **과거 입결(2022~2026) 회고형** 검색기다. 이 자료는 **2027 지원 설계(전방형)** 도구를 만들 수 있는, 성격이 다른 데이터다.

### ① 실질경쟁률 · 충원율 — 핵심 킬러 기능 (사용자 가설 검증됨)

`실질경쟁률 = (경쟁률 × 모집인원) ÷ (모집인원 + 추가합격인원)`, `충원율 = 추합 ÷ 모집 × 100`

- **3개년 합계 33,910개 연도-모집단위에서 산출 성공** (모순 332건은 제외). 2026년 기준만 12,700여 개.
- **전체 평균 충원율 128%, 절반 이상(55%)의 모집단위가 충원율 100% 이상** → 명목경쟁률이 실제 합격 난이도를 심하게 왜곡함을 데이터로 증명.
- 전체 중앙값: 명목 **7.71:1** vs 실질 **3.47:1** — 체감 난이도가 절반 이하로 내려간다.
- 검증: 화면에 표시되는 실질경쟁률 12,700건을 원자료로 역산해 전부 일치 확인(오차 0).
- UI: 카드·표에 명목/실질 병기, 산점도 축에 `실질경쟁률`·`충원율` 추가, 모순 행은 `자료 불일치`로 표기.

### ② 일정 충돌 검사 — 두 번째 킬러 기능

면접·논술 날짜와 고사시기(수능전/후)가 모집단위 단위로 들어 있음 → **수시 6장 카드를 담으면 대학별고사 일정이 겹치는지 자동 검사**하는 "지원 플래너"가 가능. 시중 무료 서비스에 드문 기능.

### ③ 전형방법 구조화

단계(1/2단계), 단계별 반영(서류100, 교과80+면접20 등) 보유 → "면접 없는 전형만", "교과 100% 전형만" 필터 가능.

### ④ 수능최저 필터

5,795행의 최저기준 텍스트 → "최저 없는 전형만" 필터는 텍스트 유무만으로 즉시 구현. (심화: `N개 영역 등급 합 M` 패턴 파싱으로 강도 등급화 — 오푸스 작업 시 정규식으로 시도, 실패 행은 원문 표시)

### ⑤ 지원자격·졸업연도 제한

`자격학년도` 컬럼(제한없음 15,321 / 특정연도 제한 ~1,650) → 재수·N수생 지원 가능 여부 원클릭 필터. 기존 `eligibility.html`과 자연스럽게 연동.

### ⑥ 4개년 모집인원 추이 + 신설 표시

2024→2027 모집인원 변화(증원/감원)와 `2027 신설` 배지 → "인원 늘어난 학과", "신설이라 입결 없는(=기회일 수 있는) 학과" 탐색.

### ⑦ 내신 반영 방식 상세

학년별 비율, 반영교과, 진로선택과목 환산 규칙, 전형총점 → 상세 모달의 "내 등급이 이 대학에서 어떻게 계산되는가" 안내 텍스트로 제공.

---

## 4. 신규 사이트 설계

### 방향

- **이름(가안)**: `2027 수시 전형설계 검색기` — 파일 `planner.html` (기존 index.html과 주소 분리: `https://maechorom31-pixel.github.io/univ/planner.html`)
- **양식·방식은 기존과 동일**: 단일 HTML + 내장 JSON, 사이드바 칩 필터 + 카드/표/산점도/상세 모달, 모바일 드로어, URL 공유. 기존 index.html의 CSS 토큰·레이아웃을 재사용해 패밀리룩 유지.
- **자료·주소는 다르게**: 데이터는 `data/mojip2027.json`(문자열 사전 스키마), 빌드 시 HTML에 인라인 임베드.

### 화면 구성

1. **사이드바 필터**: 지역 / 계열 / 전형 카테고리(교과·종합·논술·실기: 세부전형명에서 파생) / 대학·학과 검색 / **면접 유무 / 수능최저 유무 / 졸업연도 제한(재수생 가능) / 신설 포함 여부** / 내 등급(선택형, 기존과 동일 UX)
2. **카드 보기**: 대학·전형·모집단위, 2027 모집인원(전년 대비 증감 화살표), 명목 vs **실질경쟁률**, 70%컷 3개년 미니 추이, 최저·면접·신설 배지
3. **표 보기**: 정렬 가능(실질경쟁률·충원율·70%컷·모집증감)
4. **산점도**: 축 선택에 실질경쟁률·충원율 추가
5. **상세 모달**: 전형방법(단계별), 지원자격 전문, 수능최저 전문, 내신 반영 방식, 일정(면접/논술/발표), 3개년 입결·추합 차트
6. **🗂 지원 플래너(신규 탭)**: 관심 전형 담기(최대 6+α) → 수시 6장 구성표 + **일정 충돌 자동 경고**(같은 날 면접/논술), 수능전·후 구분 타임라인, localStorage 저장 + URL 공유

### 데이터 스키마 (`data/mojip2027.json`)

```
{ schema: "mojip2027-v2",
  meta: {...},              // 출처·기준일·행수·정제 시 버린 값 목록
  strings: [...],           // 텍스트 값 사전 (6,559개)
  columns: [56개 컬럼명],    // rows 원소 순서 (원본 55개 + 특이표기)
  textCols: [...],          // 값이 strings 인덱스인 컬럼 목록
  rows: [[...], ...] }      // 16,873행, null = 결측
```

로딩 코드는 `textCols`에 속한 컬럼만 `strings[v]`로 역참조한다. 실질경쟁률·충원율은 **클라이언트에서 계산**하며, 2절 5·6항의 안전장치를 반드시 함께 적용한다.

---

## 5. 구현 결과

모두 구현·검증 완료. 실제 브라우저(Chromium)로 기능·데이터 정합성·모바일 레이아웃을 확인했다.

### 파일 구성

| 파일 | 역할 |
|---|---|
| `src/planner.tpl.html` | 화면·로직 원본 (데이터 자리표시자 `__DATA__`) |
| `scripts/parse_2027.py` | 엑셀 → `data/mojip2027.json` 정제 |
| `scripts/build_planner.py` | 템플릿 + JSON → `planner.html` (4.18MB 단일 파일) |
| `planner.html` | 배포본. 파일 하나로 열리고 링크로 공유 가능 |

재생성:

```bash
python3 scripts/parse_2027.py <엑셀경로> data/mojip2027.json
python3 scripts/build_planner.py
```

### 구현된 기능

1. **필터**: 지역(17) · 계열(4) · 전형 카테고리(교과/종합/논술/실기/기타) · 대학·모집단위 검색(한글 IME 안전) · 내신등급 ±범위(선택형)
2. **조건 칩**: 최저없음 · 면접없음 · 논술전형 · 재수생 가능 · 2027 신설 · 인원 증가 · 실질경쟁률 3:1 이하
3. **카드**: 명목 vs 실질경쟁률 병기, 충원율, 70%컷과 3개년 인라인 스파크라인(SVG — 차트 라이브러리 불필요), 2027 모집인원 증감 화살표, 배지
4. **표**: 12열, 헤더 클릭 정렬, 상위 500건 표시(초과 시 안내)
5. **산점도**: X·Y축을 70%컷 / 실질경쟁률 / 명목경쟁률 / 충원율 / 모집인원 / 모집증감 중 선택, 카테고리별 색, 점 클릭 시 상세
6. **상세 모달**: 3개년 경쟁률·입결·추합 표, 모집인원 4개년, 전형방법(단계별), 일정, 수능최저 전문, 학생부 반영 방식(학년별·교과별·진로선택), 지원자격 전문
7. **🗂 지원 플래너**: 담기(localStorage 유지) · 수시 6회 초과 경고 · **대학별고사 일정 충돌 검사** · 수능전/후 타임라인 · 요약표 · 링크 공유
8. 검색조건·플래너를 URL 해시로 공유, 인쇄 스타일, 모바일 드로어, `/`·`Esc` 단축키
9. **과거 입결 상호연결**: 상세 모달에 2022~2026 등급 70%컷과 `index.html` 링크, 역방향으로 `index.html` 모달에도 2027 모집요강 링크. 이름 표기가 다른 두 자료를 정규화로 이어 16,873행 중 14,780행(87.6%) 연결 (`scripts/build_xref.py`)

### 일정 충돌 검사 판정 기준

원본 일정은 `11.30(월)` 같은 단일 일자와 `11.25(수)~11.27(금)` 같은 기간 공지 두 형태뿐이다(전부 파싱 성공, 면접 3,904건·논술 1,294건). 이를 구분해 두 단계로 판정한다.

- **⚠ 확정 충돌** — 서로 다른 대학이 **단일 일자**로 공지한 고사가 같은 날. 둘 다 응시 불가.
- **△ 확인 필요** — 한쪽이라도 **기간 공지**이거나 **같은 대학** 안에서 겹침. 기간 공지는 그 안에서 하루가 배정되고, 같은 대학은 시간대를 나누는 경우가 많다.

일정 정보가 없는 전형은 검사에서 제외하고 그 개수를 화면에 밝힌다.

### 검증 기록

| 항목 | 결과 |
|---|---|
| 실질경쟁률 역산 대조 | 12,700건 전부 일치 (오차 0) |
| 70%컷 등급 축 오염 | 13,725개 값 전부 1.0~9.0, 환산점수 혼입 0 |
| 숫자 필드 타입 검사 | 문자열 혼입 0건 |
| 충돌 검사 (확정) | 가천대(글로벌)·가천대(메디컬) 11.30 논술 → 정상 감지 |
| 충돌 검사 (확인 필요) | DGIST·경상국립대 11.25~ 면접 기간 겹침 → 정상 분류 |
| 6회 초과 경고 | 7개 담기 → 경고 표시 |
| URL 공유 왕복 | 필터·플래너 상태 복원 확인 |
| 필터 정확성 | 최저없음·면접없음·재수생 가능 각각 전수 조건 만족 |
| 모바일 390 / 360px | 가로 넘침 0, 탭 줄바꿈 없음 |
| JS 런타임 에러 | 없음 |

### 시뮬레이션 & 오류검증 (2차)

브라우저에서 사용자 조작을 무작위로 반복하며 깨지는 곳을 찾았다. 스크립트는 `sim.mjs` 계열(작업용, 저장소 미포함).

| 항목 | 방법 | 결과 |
|---|---|---|
| 파생값 정밀 대조 | 300행 × 3개년 실질경쟁률·충원율·증감 전수 재계산 | 불일치 0 |
| 정렬 | 9종 정렬 각각 전체 결과 단조성·결측 후순위 검사 | 전부 정상 |
| 필터 | 무작위 200조합(지역·계열·카테고리·조건칩·검색어·등급) 결과 전수 검사 | 조건 위반 0 |
| 모달 | 400개 행 열어 예외·`undefined`/`NaN` 노출 검사 | 0건 |
| 플래너 | 무작위 100회 담기 후 충돌 판정 자체 검증 | 오판 0 |
| 충돌 검사 | 단일 일자 논술이 겹치는 모든 대학쌍 전수 | 미탐지 0 |
| URL 상태 | 무작위 20회 직렬화 → 새 탭 복원 대조 | 불일치 0 |
| 출력 이스케이프 | 학과명에 `<img onerror>` 주입 | 실행 안 됨 |
| 반응형 | 360·390·768·1024·1440·1920px | 가로 넘침 0 |
| 저장값 | 새로고침 유지 / 손상된 localStorage 방어 | 정상 |
| 상호링크 | planner ↔ index 실제 이동 후 결과 존재 확인 | 양방향 정상 |

### 시뮬레이션에서 찾아 고친 문제

**차단된 CDN이 페이지 전체를 13초 멈춤 (심각).**
`index.html`과 `planner.html`은 Chart.js를 `<script src>`로 걸어 두고 있었다. CDN이 막힌 망에서는
응답을 기다리느라 화면이 **13.5초** 멈췄고, `index.html`은 끝내 실패하면 `Chart.register`에서
스크립트가 끊겨 **검색조차 되지 않았다**. 같은 크기인데 CDN을 안 쓰는 `eligibility.html`이 0.18초에
뜨는 것과 대조된다. 학교망 필터링을 생각하면 실제로 겪을 수 있는 상황이다.

고친 방법 — 화면을 다 그린 뒤 라이브러리를 비동기로 받아오고, `index.html`에는 아무 일도 하지 않는
대체품(stub)을 먼저 올려 두어 5곳의 `new Chart()` 호출이 그냥 통과하게 했다. 진짜 라이브러리가
도착하면 교체하고 다시 그린다.

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| `planner.html` | 13,300ms | **710ms** |
| `index.html` | 12,900ms (이후 기능 정지) | **620ms** (전 기능 동작) |

**학과 이름만으로 이으면 다른 학과가 합쳐짐.** 괄호를 모두 지우는 정규화에서
`경희대 한의예과(자연)`이 `한의예과(인문)`의 과거 입결에 연결됐다. 계열·야간·남녀·수업연한
구분자는 키에 남기도록 고쳤다. (검증: 키 충돌 0건)

**링크로 도착해도 검색칸이 비어 보임.** `index.html`이 `#q=`를 필터에는 반영하면서 입력칸에는
넣지 않아, 링크를 타고 온 사용자가 왜 결과가 걸러졌는지 알 수 없었다. 입력칸에도 반영하도록 고쳤다.

### 남은 선택지 (필요하면 추가)

- 수능최저 텍스트(5,795건)를 `N개 영역 등급 합 M` 패턴으로 파싱해 **최저 강도**를 등급화 → 지금은 유무 필터와 원문 표시만 제공
- 플래너에 내 등급 대비 **소신/적정/안정 자동 분류** 추가
- 연결하지 못한 12.4%의 학과 — 이름이 크게 바뀌었거나 신설·통합된 경우로, 별칭표를 손으로 채우면 더 이을 수 있다
